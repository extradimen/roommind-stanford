# G5全新场景家族评分验证协议

起始日期：2026-09-12。最新状态见文末按日期追加的执行记录。

## 目的

验证维度隔离评分契约能否在未参与提示设计的材料上稳定工作。该验证只评估评分协议，不能证明
RoomMind架构效果，也不能替代后续2×2组件因果研究。

## 防止开发集泄漏

- 旧谈判、市场发布、候选人面试、事故响应及其世界版本全部视为开发家族。
- G1–G4.15所有转录、v1–v4预测、Codex单一专家标签和新语义复评均不得进入新场景内容、
  标签模板或评分提示。
- 新家族在任何对话生成前登记名称、角色类型、事实结构、程序结构、停止规则和哈希。
- 冻结后不得按评分结果替换家族、删案例或修改标签定义。

## 拟议的新家族框架

1. 公共图书馆馆藏空间分配：馆长、无障碍顾问、青少年项目负责人、档案员。
2. 研究联盟数据发布审查：首席研究员、隐私负责人、复现审查员、合作机构代表。
3. 博物馆借展与保护决策：策展人、保护科学家、保险代表、借展方协调员。
4. 社区交通班次调整：调度负责人、乘客权益代表、司机代表、预算分析员。

这些家族不得复用旧家族的专名、数字、日期、义务图或逐句模板。每个家族至少冻结两个独立世界
实例；样本单位是家族，不把同一对话的六个评分当作六个独立样本。

## 评分与参考顺序

1. 冻结场景、角色、共同世界、条件分配和模型绑定。
2. 生成并封存转录，保存原始事件、回执和哈希。
3. 生成只含盲化材料的参考包；评分器不得看到条件。
4. 先冻结独立参考判断，再运行固定评分器；禁止根据评分器输出修改参考。
5. 技术失败仅补算缺失维度，已完成结果和转录哈希不得覆盖。
6. 报告逐维一致、混淆、弃权、技术失败和条件猜测；不使用单一总体分数掩盖退步。

评分器验证与架构效应筛查分开：第一阶段每个世界只生成一条对话，A/B/C/D各分配两条，共8条；
这个小组只产生48个“场景×维度”开发判断，条件不是参考标签，也不估计架构效应。评分门通过后，
第二阶段才建立每世界四臂完整区组，共32条对话，用于候选选择；它仍不是正式确认研究。

若仍由Codex单一专家提供参考，必须继续标为AI开发诊断，不能称为人类准确率。正式确认门仍需
独立人类参考、预声明阈值和未用于开发的新一批家族。

## 当前门槛

- 维度隔离契约本地验收必须全部通过。
- 四个新家族各两个世界实例已经在生成前冻结，框架摘要为
  `766f9dfb14dae5fd9fa576481a9c950f3a52bd9dcba7157c06e27d4fdb9ce352`；尚未生成对话。
- 8个世界均已冻结条件中立角色包，每个世界1个固定玩家角色、3个NPC；角色包摘要为
  `61e102a9de723cd947e45516979dd6f4de79c0b97bf5ed1b42336c05a302d0a4`。
- 8条盲化开发验证的平衡分配已经冻结，A/B/C/D各2条；计划摘要为
  `cc1d4fd9f89173fd2936e583a995d31e15143100e2f659911114de97b54357ec`。该计划明确禁止推断
  架构效果，并保留`source_revision=null`和`model_binding=null`，因此尚不可执行。
- 清单基础设施现可逐场景绑定不同角色表；旧清单若没有该字段，仍按旧共享哈希路径重建。
- 新对话生成、Ollama Cloud调用、推送、部署及线上批次均未获本协议自动授权。

## 生成前v2纠正

v1在外发前审计中被判定认识论与角色策略区分度不足：所有核心世界事实均公开，角色目标过于通用。
v1没有生成任何对话，全部文件和哈希保留为被否决的开发历史。v2为每个世界新增3个角色可见且可
披露事实、1个角色可见但受保护事实，并给四个角色分别冻结不同策略目标；条件标签仍不可见。
v2框架、角色包和8条平衡计划摘要依次为
`a7d0bfa98450acdc6076e7100d9752cfa9f23c44dbd5785abe985bf8703a648e`、
`7bae19f2151f8ef298aee78abab4fd63c58480fddf659fbad4354ac8e6dfff53`和
`6070c945965d61c4f29797b606cf8425ee50f1473096635e55e2c1ac2ac5c2fd`。v2同样尚不可执行。

## 2026-09-12 执行授权与入口

用户随后明确授权：仅提交、推送及部署本轮G5代码/文档/研究制品，并把v2的8个内部场景、
角色设置和逐轮对话上下文发送到固定的`ollama/gpt-oss:120b`，生成8条盲化开发验证对话。
授权不包含48项评分器调用、32条完整区组筛选、确认研究或真人评审。

预生成文件仍保留`launch_authorized=false`，避免事后改写冻结历史。新增
`fresh_execution_v2`在运行时另建绑定，必须记录精确部署revision、逻辑endpoint、模型、授权ID、
16步/1次修订上限、共同请求字节预算及8个选择分配；完整32分配manifest只用于让共同Runtime
验证条件，执行器只运行预先冻结的一世界一臂8条。输出采用一个可恢复SQLite事件库，每条完成后
立即冻结并以排他新文件保存内部source；全部完成后才生成公共转录包。评分和完整区组入口不存在，
不会由本命令串联启动。

新增入口的提交前验收为390项（含48项PostgreSQL）全部通过，无失败、错误或跳过；回执摘要
`dc005031fa8032b6ddd00835881b178eb3c650d021185e9e6b9454d31f0e6b08`，交接摘要
`af521e107b9fd50c2ff5a4d7e911f9e5b446e5e6334717a9490c9def9f37ac41`。第一次受限沙箱运行保留为
失败回执：342项通过，48项因不能访问本机PostgreSQL socket报错；随后在获准环境完整复跑，
没有覆盖或把环境失败解释为代码通过。

## 2026-09-13 v11完成与评分预检

在完整保留v7、v8、v9及v10失败历史后，v11完成8/8条开发对话。四臂各2条、每条16个事件，
共128个事件、1,444条模型尝试、8个最终密封、0次重开；SQLite完整性检查为`ok`。执行绑定
摘要为`1f6b4e7b030bc608df66c3274f6adbfc7e3d9c9abc0967912840968d7dde4fd4`，公共转录内容摘要为
`03f9ebf1d1a701a8f42199051ae9758b4804dda54edc5c97536d4fc37bff5a48`。该批只证明当前执行链
能够完成固定开发任务，仍不得估计架构效应或研究效力。

本地评分预检已从8个密封源重新构建48个“场景×维度”任务，并生成两份不同顺序、匿名编号的
独立参考审阅包。评分输入摘要为
`ac09d92494c00ad9994b547017b113b344eafbed7a645e331d756db8b7f89ae0`，预检摘要为
`3a96d8b8d78ffcf84e4978103fafbdfc870616dad5112f4487d8a81baeabc7df`。预检没有模型调用、没有
参考标签、没有真人分配，也没有外发。当前对话中的Codex已经接触过条件分配，不能被登记为
真正条件盲化的独立参考者；如提供判断，只能另存为可能知情的AI开发诊断。

48项Ollama评分执行器已准备为显式授权文件门控：授权必须绑定上述输入摘要、固定
`gpt-oss:120b`、`https://ollama.com/api/chat`、最多96次请求（每项仅技术失败可重试一次）、
保存全部原始响应，并明确排除32条筛选。当前没有这样的授权文件，因此评分请求尚未发送；
32条完整区组筛选也未启动。

## 2026-09-13 v11评分结果与暂缓决定

用户按上述精确范围授权后，先由一个新的独立Codex任务仅读取匿名`reviewer-a.json`，冻结48项
单一AI专家开发参考：18项`clear`、24项`violation`、6项`uncertain`，文件摘要为
`96f289023dbb2fa60cb1fd9d49b292802753d9342cc78723e4af0ef415fb5dc0`，评分前冻结摘要为
`b254e54a449da39985de12c633c045ba4b340b3a8b0416b2cc374172ff1f4b7a`。任务记录显示它只读取
匿名包；它仍是AI开发诊断，不是真人金标或人类准确率依据。

随后48项固定评分全部完成：47项首轮成功，1项因引用数量不合法形成永久失败尝试，按预定规则
仅重试该项并成功；共49次请求，最终48项完整、0弃权、28项`clear`、20项`violation`。离线审计
摘要为`38de69a28d83d21ef523b388385e99a47a337e180716b958c9b9f3c7602a151a`。

与评分前参考比较时，42项有确定参考标签，其中28项一致（66.7%），4项误报、10项漏报；确定
参考条件下误报率22.2%，漏报率41.7%。逐维确定参考一致率为：角色策略20.0%、认识论50.0%、
互动结构66.7%、程序71.4%、时间75.0%、多人动力学100.0%。比较摘要为
`546b1f973b5dfbd8c01341c36776095f25c65e6d6c511c1c1e7819be0ff62529`。每维最多只有8个共享
对话单元，不能把48项当作48个独立样本。

四臂残余盲化猜测以对话为单位：8段中3段选择不确定，全部8段命中2段；5个方向性猜测命中2个，
且置信度为0.35–0.55。该小样本只说明未观察到明显条件泄露，不是盲化等价证明；审计摘要为
`7a6894c392ae4cb01b53807cf3281dd05e2695adb9b805a019b2dd7ca2109d2c`。

评分前没有冻结数值通过阈值，因此不得事后选择门槛并宣称正式“过门”。同时，角色策略和认识论
维度的大量冲突以及总体41.7%的确定参考漏报，不能为直接进入32条筛选提供充分依据。当前决定是
暂缓32条筛选，先裁决14项确定参考冲突并判断误差来自评分器、单一AI参考或两者。已生成隐藏条件
和评估来源身份的14项裁决包，包摘要为
`62182130e5232f2bf80d022098a1783a64d5152e0f2b11ed99fc128ababaea93`。

第二个独立Codex任务随后仅读取该匿名包，完成14/14项裁决；任务记录显示未读取协调表、条件、
来源文件或评分结果，也未调用网络。解盲后，13个方向性裁决中12个支持冻结参考、1个支持评分器，
另1个时间维度冲突为不确定。评分器原有10个确定参考漏报全部由裁决支持参考；4个误报中2个支持
参考、1个支持评分器、1个不确定。裁决文件摘要为
`bab875c550063f85aa94fc4edd589ee80ec4f97e52881ea6bb8dee41e429e884`，离线裁决审计摘要为
`eca7f32b653dc4539a7f1282637bce623700398be4f168567215d0427fc86293`。这仍是单一AI开发裁决，
不是真人金标或确认性证据。

据此，不保留当前评分器原样进入筛选。下一步是修订状态更新、不透明信号、完成标志与实质结果、
维度隔离和祈使请求处理规则，并同步澄清参考标注说明。v11继续作为已暴露开发数据，不能用于证明
修订后的评分器有效；新版本必须在预先冻结的未见材料及前瞻门槛上验证。32条筛选保持暂停。

## 2026-09-13 评分器v3与前瞻验证门

评分器v3已在本地实现，新增对权威回执状态更新、完成标志的有限含义、不透明信号不得被量化、公开
声称不得新增程序门槛、维度隔离及祈使信息请求的明确规则。v3语义契约摘要为
`490ff3f1a9cb6c167b3c01d44672a7817ef1da6f8df0dc71c181565c987f8f57`，目录式评分提示摘要为
`cb5056dc8951b1f0188e98ed735409d5b53cfa7e4f1d6fefd20f5aa4d40ce00e`。v2契约和提示仍保留，
v11的48个旧请求已经通过逐字重建测试；旧结果没有被新规则重算或覆盖。

在生成任何新验证材料前，已冻结评分器v3的前瞻验证计划，摘要为
`806c776d6452177cef4222c9d8d9c2327ed8aeb0bc11c206893d4c6e521cde66`。计划要求四个新领域、
每领域两个世界、共8条对话和48个评分单元；两名以上独立盲评者先形成参考，分歧须在看到评分器
输出前独立裁决。全部门槛同时成立才可通过：48/48技术完成、至少36个确定参考、确定参考总体一致
率至少85%、误报率和漏报率各不高于15%、每维至少6个确定参考且一致率至少75%、评分器弃权率不
高于15%。任一失败则保留全量记录、继续暂停32条筛选，并在另一套未见材料上验证下一版。该计划
只是本地冻结，不授权新对话、外发参考包、云端评分或32条筛选。

上述门槛冻结后，已创建与v11四个领域完全分离的本地材料：海岸避难所启用、野外考察出发、城市
供水告知和微电网服务移交，每类两个世界。旧新领域、角色ID及角色姓名均通过不相交测试；每个
世界仍含三个可披露的角色信号和一个受保护事实，A/B/C/D各预分配两条。框架、角色包和8条
分配计划摘要依次为`b4535a7beece985c630c23653f3205a021c99608b4697ab84b11c0d0968eea37`、
`083178129d978918bbd4ac3146f519a43938bca2e06ac8e373c032321fe02125`和
`1b5f48ef2d5532bd9330fe61a7c6fdd241541c6768adbb468360b0bcb924d0d0`。本地执行绑定和可恢复
入口已准备，固定8条、每条最多16步，且明确关闭评分和32条筛选。当前仍未绑定实际部署提交与
外部授权，所以没有生成新对话或发出任何新模型请求。

## 2026-09-13 v12失败与v13恢复

v12按授权启动后在第一条对话完成并密封、第二条完成3个事件时停止。失败原因是认知规划响应包含
两个顶层目标但没有必要的子意图，触发`Goal requires child intentions`。失败现场完整保留：SQLite
完整性为`ok`，含19个已提交事件、194条尝试、1个密封源；v12执行绑定摘要为
`c5a5434bfb5157f8dda6d89b0fe3e8082510b8fd7db58214ff64688cbcfcb9cd`，第一条密封源摘要为
`4a81d822f60c996348cc190db7f5e8aeefb9871ad8d15076e946f62d7da6e2a4`。该批固定记为1/8失败批次，
不得与后续批次拼接。

根因是v12新入口默认结构化修订次数为0，遗漏了v11已验证的`reasoning_effort=low`和最多3次结构化
修订配置。v13只恢复这两个执行可靠性参数，继续使用完全相同的v3框架、角色包、8条均衡分配、
16步停止条件、模型和请求预算，并把上述v12执行绑定摘要作为精确前序。v13仍不授权评分、32条筛选
或确认研究。

## 2026-09-14 v13失败与v14修复候选

v13已正确启用3次结构化修订，但在第一条对话密封、第二条完成12个事件后停止。问题标注器连续
4次都返回`kind=question`，同时错误附带只允许用于`kind=response`的`question_id`字段。由于旧修订
反馈只写“字段无效”，模型反复生成同一种错误。失败现场完整保留：SQLite完整性为`ok`，含28个
已提交事件、320条尝试和1个密封源；v13执行绑定摘要为
`a4c7e6d50bbbdbe026e3177da2e13b7feadf94138c97fc6b33c60b3f711105a4`，第一条密封源摘要为
`8bcfd812a9bc7da96ffce28a637437b4afd4dd13d2e91280778c4b502bfaa21f`。该批固定记为1/8失败批次。

v14候选不自动删除字段或推断模型意图。它仍然拒绝非法输出，但把缺少字段、多余字段以及question和
response各自允许的精确字段写入下一次修订反馈，使模型能够修正已被明确识别的格式错误。此变化
适用于共同问题标注器，在所有四臂中一致；场景、角色、分配、模型、停止条件和评分隔离不变。

## 2026-09-14 v14完成与评分预检

v14已完成8/8条开发验证对话。A/B/C/D各2条，每条16个事件，共128个事件、1,424条模型尝试、
8个密封源和0次重开；SQLite完整性为`ok`。执行绑定摘要为
`89ccb30ec547c1542597f3c16aca143905b6c15fd3e604a951877f4d68e49354`，公共转录内容摘要为
`f9a626e5ba3be2310d955d3ff2c5d16e397cb8562b20c788409ce88563c72994`。八个内部源均已在本地逐一
复验世界规格、角色输入、停止条件、事件哈希链、回执、尝试预算和最终封印，源内尝试总数与SQLite
完全一致。

结构检查未发现受保护值进入公开发言；每个角色均有公开行动，所有注册操作和权限由离线源验证器
复算。一个微电网对话含4次完全相同的公开发言重复，两个对话在16步截止时各保留1个未完成问题；
这些是后续盲评需要判断的对话质量现象，不作为技术失败删除，也不据此筛选样本。

评分前本地预检已固定全部8条对话和六个维度的48个任务，并生成两个不同顺序的匿名独立评审包。
评分输入摘要为`3c6a948f396fa7d68426df10b506f342188eccf8b57088701c7e841e11744f48`，预检摘要为
`1ff927a8488a995e5e893cc8a286fee6c1070b47316635121409586e2a8b370f`。评审包不含条件、源运行ID或
模型评分；当前参考标签为0、模型评分为0、外部分发未授权。评分器仍绑定v3提示和语义契约，摘要
分别为`cb5056dc8951b1f0188e98ed735409d5b53cfa7e4f1d6fefd20f5aa4d40ce00e`和
`490ff3f1a9cb6c167b3c01d44672a7817ef1da6f8df0dc71c181565c987f8f57`。32条筛选继续暂停。

两名相互隔离的Codex AI评审者随后各自完成48/48项，A为23项clear、25项violation，B为25项
clear、23项violation；40项一致、8项分歧。两份结果在模型评分目录建立前冻结，摘要为
`b9e403f1129a44a5bf222d127c4ae395fa4432e54fa6b6312bb735d1c9a7a045`。第三名Codex AI裁决者只读取
匿名X/Y分歧包，在不知道条件、评审身份及模型评分的情况下完成8/8项裁决。最终参考含22项clear、
26项violation，六个维度各有8项确定标签；摘要为
`d2c4420fc6d7241b315bd6ff53c0b0663ffcb60403c76c224e74a6c8f045e934`。这些是AI开发参考，不是真人
金标，也不能估计人类准确率。

用户随后明确授权发送48项冻结评分任务至Ollama云端，最多96次请求、保存原始响应，并继续排除
32条筛选。授权记录摘要为`b0151e890826b2b75efe04970dbfa6308dd170b0c6e56e3074914a3072f2d8ba`。

## 2026-09-14 v14评分器前瞻门结果

48项任务共发出50次请求。47项取得最终预测；一个程序维度任务首轮引用了不存在的证据ID，重试后
成功。一个微电网时间维度任务连续两次引用不存在的证据ID，按预定两次上限保留为最终技术失败。
最终结果为34项`clear`、13项`violation`和1项技术失败；原始HTTP响应、开始记录和解析结果均已
保存。离线审计摘要为`d8c1e80b570a5864fd10d8c6dd6bfcd0d3a11a82539b5faffdf7353a71ceac75`。

与评分前冻结的双AI参考比较，评分器一致34/48（70.8%），真阳性13、真阴性21、误报0、漏报13；
误报率为0%，漏报率为50%。六维一致率依次为：认识论75.0%、互动结构87.5%、多人动力学87.5%、
程序62.5%、角色策略50.0%、时间62.5%。14项不一致或失败中，13项都是参考判违规而评分器判
`clear`，另1项是参考判`clear`但评分技术失败，显示主要问题是漏检而非误报。

按摘要为`806c776d6452177cef4222c9d8d9c2327ed8aeb0bc11c206893d4c6e521cde66`的前瞻门，
本批同时未满足48/48完成、零最终技术失败、总体一致率至少85%、漏报率不高于15%以及每维一致率
至少75%五项要求，因此评分器v3资格测试失败。比较与门判定摘要为
`d50bb9eeadf540d28de3e12a1de945e12c6b381e9461db86bedf3fcf2ad74c31`。该结果属于AI参考下的
开发诊断，不能估计人类准确率或架构效应；32条筛选继续暂停。

## 2026-09-14 评分器v4与下一套未见材料

评分器v4针对v14的13个漏报开发项补充六类判定：角色必须根据当前权威状态推进其明确目标；
`visible_to`限制直接知识和监测能力；前后状态必须按发生时间解释；无新信息或有效交接的语义循环属于
互动缺陷；单纯轮流发言不等于实质多方参与；操作顺序正确也不能替代目标操作的完成和对成功回执的
承认。证据目录提示还要求逐字复制证据ID。若首轮只发生严格格式错误，唯一一次修复请求必须包含
原始失败响应、明确错误代码和完整合法ID列表，且仍不允许静默删除或改写字段。

旧v3契约和提示保留，摘要仍为`490ff3f1a9cb6c167b3c01d44672a7817ef1da6f8df0dc71c181565c987f8f57`
和`cb5056dc8951b1f0188e98ed735409d5b53cfa7e4f1d6fefd20f5aa4d40ce00e`，v14请求逐字重建测试通过。
v4契约、提示和修复说明摘要分别为`57d5ccf97ce8b1b05095653b2ed28f12cbba32cff187177c73117e99912e1faa`、
`ac969276d4cd7f3de35d297cd702f72858adbec88d9096f58281a0e5e30d77cc`和
`d4b7234dba768b4e02331bdd6a2aace425de8f62f7a9c746b25c9b358b84589d`。

在创建下一套材料前，已按完全相同的数值门槛冻结v4前瞻计划，摘要为
`7967fac8a92743fe17e457a54fa6d5d20f266032d24c6e9d3a344959959370f0`。随后创建四个新领域：果园
防霜响应、档案藏品转移、卫星地面站过站和学校餐食过敏原召回，每类两个世界，A/B/C/D各2条。
它们的领域ID、角色ID、角色姓名和公开数字与v11、v14集合不相交。框架、角色和分配计划摘要为
`27f049c0a57a4ede71bcafeedf23681e84539801584ca11aa88f09f55cec8be2`、
`1417777806e9281db3253fc617026c1ab1a10e597bbe8aa6313cf11258cd9416`和
`e2a24faa87702662509538b72c7f328755c49c063b15c0b2b854df4c315fff78`。当前只完成本地准备，未授权
生成新对话、外发盲评、云端评分或32条筛选。

## 2026-09-14 v15执行失败与v16恢复候选

随后取得的授权只覆盖把上述8条开发对话发送至Ollama云端模型并保存原始响应，不覆盖AI参考、
评分或32条筛选。v15在完成并封存5/8条后，于第6条卫星地面站对话的第3个事件失败。失败批次保留
6个世界、83个已提交事件、902条模型尝试、5个封存和零次重开；SQLite完整性检查为`ok`，执行绑定
摘要为`0a121e25e2c4bc3aa1d277dab58e507d3f269aed64613aedeaa5bc50dcd16463`。原始模型尝试、已生成来源、
日志和SQLite均保存在独立的`v15-development-online-failed`目录，不能与后续批次合并。

失败发生在`payload_coordinator`的层级计划更新。四次候选依次违反“新目标需要子意图”、使用不可用
操作、再次缺少子意图，最后一次则把一个合法来源ID漏掉末尾字符。最终错误为
`Updated task cites evidence outside supplied context`。这说明严格来源校验正确阻止了错误提交；暴露的
执行问题是修复反馈没有足够明确地要求模型逐字复制完整ID。

v16恢复候选仅增强失败反馈：返回完整合法来源ID并明确要求逐字复制，同时重复新目标必须带非目标
子项和不执行时操作名为空字符串的规则；结构化修订上限由3次增至4次。模型、推理强度、8个世界、
四臂分配、每条16个事件、停止条件和评分器v4前瞻门均不改变。v16必须使用新目录完整重跑8条，
并在执行绑定中引用v15失败批次摘要；v15的5条已完成对话只作失败归档，不计入v16进度。

## 2026-09-14 v16执行失败与v17恢复候选

v16完成并封存4/8条后，于第5条果园防霜对话的第3个事件失败。失败批次保留5个世界、67个已提交
事件、678条模型尝试、4个封存和零次重开；SQLite完整性为`ok`，执行绑定摘要为
`74c402748592cf17867a48dcb5787bed8405a2f68589727783e100d743114a6a`。全部原始响应、来源、日志和
SQLite保存在独立的`v16-development-online-failed`目录，不能与任何恢复批次合并。

这次模型遵循了v16的逐字复制要求。真正原因是程序给模型的修复允许列表来自完整私有记忆库，而
增量更新校验器只接受本轮实际检索并提供给模型的来源；因此反馈声称合法的部分ID会被下一层正确
拒绝。v17把修复允许列表和任务完成证据列表统一为本轮检索集合，完整记忆库仍只用于复核旧计划中
已经固定的来源。该修改修正反馈与校验契约，不扩大模型可见证据，也不改变模型、材料、四臂分配、
16事件上限、停止条件、评分器v4或前瞻门。v17必须在新目录完整重跑8条，并绑定v16失败摘要。

## 2026-09-14 v17开发对话完成

v17完整完成并封存8/8条开发对话，A/B/C/D各2条，每条16个事件，共128个已提交事件、1,434条
模型尝试、8个封存和零次重开。SQLite完整性为`ok`；8份来源均通过清单与封印复核，转录中的世界、
封印和事件数逐条对应。执行绑定摘要为
`b95cbe3786761ded454fc36ab5e40a0b771698cb5f6a78779733cbf7cfc92ec6`，转录摘要为
`8900620420a654df6326de9fbfe9015740f73b93bb244b5a1bbfb0c71f0080c2`，离线审计摘要为
`aa9b9844414233e52b0d5e451fa41542b9dfc6fe406f0068b1284b7d1f4c66d2`。

至此8条开发对话生成阶段完成。v15和v16仍作为失败批次独立保留，不与v17合并。尚未为v17创建
独立AI参考或运行评分器v4；48单元评分器资格门和32条四臂筛选仍未执行。

## 2026-09-15 v17独立AI参考冻结

在任何评分器v4调用之前，已从v17的8条封存对话生成48个维度任务和两份独立随机顺序的盲评包。
评分输入摘要为`ea35300e0680a1146f0daf998a3d28ebfe786cdf587cd6e65430b97e589c68ea`，评分器提示、语义契约和
修复说明与v4前瞻计划逐项一致。两名AI评审分别给出35项明确、13项违规和30项明确、18项违规；
两者一致41/48，7项分歧由第三名AI在隐藏评审来源、四臂条件和评分器输出的情况下裁决。

最终48项全部为决定性标签：30项`clear`、18项`violation`，六个维度各8项。最终参考摘要为
`a7b9dad9ede1d5cdab753be841b8c47551c89ed2eefdced2cc9797b892a89a30`。该参考仅是AI开发诊断，
不是人类金标准，不能估计人类准确率或支持确认性结论。评分器v4尚未调用，32条筛选仍暂停。
