# RoomMind 重大架构迭代记录

本文是RoomMind架构演进的长期索引。每次发生会改变系统研究假设、运行方式、评价方式或多人仿真语义的重大调整，都应在此新增一轮；普通缺陷修复不单独编号，但应归入对应轮次的“问题与验证”。

## 2026-09-10：G5 架构与可归因实验重构（本地实施中）

**2026-09-12 维度隔离与全新家族门：** 新语义复评在旧开发集上整体一致率下降后，没有继续
针对同48项追分。评分契约升级为v2：共同证据语义与六个互斥维度范围分离，每次请求只携带当前
维度范围，并禁止把其他维度的缺陷迁移进来；旧短维度名显式映射到G5规范名，完整v1契约和提示
仍可逐字重建冻结请求。v2摘要
`86157439752f7dba607de9af1d74acfa3ece4f997ed89f775d358bc6646f684d`；定向26项及完整
SQLite/PostgreSQL 371项通过，验收摘要
`f6ab6e36df0009cd7c23e8eef7ccd7a391365a0f0f6d1823420f673ce218198f`。既有holdout审计确认8个
旧家族全部有开发暴露，`registered_exposure_free=false`且语义新颖性未建立，因此不能从旧批次
伪造留出集。下一验证必须在生成前冻结全新的图书馆、研究联盟、博物馆借展和社区交通家族。
本阶段没有新增外部调用、推送、部署或线上批次。

四个全新家族的具体框架随后已冻结：图书馆空间分配、研究数据发布、博物馆借展保护及社区交通
调整各两个独立世界，共8个不同快照；每个世界有4个注册角色、角色限定动作、明确前置条件和
结构化模拟回执。框架摘要
`766f9dfb14dae5fd9fa576481a9c950f3a52bd9dcba7157c06e27d4fdb9ce352`。纳入框架后的完整
SQLite/PostgreSQL 374项验收全部通过，摘要
`f0eaad3c1a5130560019715011c50184d574995dffb4fb52962a76ccc76c766f`。对话数和标签数仍为0。

8个世界的条件中立角色包也已冻结，每个世界固定1个玩家角色、3个NPC，摘要
`61e102a9de723cd947e45516979dd6f4de79c0b97bf5ed1b42336c05a302d0a4`。角色包不含实验条件名称，
共同指令要求只使用可见事实、只执行获授权动作并允许不确定、拒绝和未解决收尾。最终完整
SQLite/PostgreSQL验收376/376通过，摘要
`c96e9f682d9aaacf1c827f82595c891e7a3f2c0964a7343f051446f29aaa4639`。

**2026-09-12 新语义契约开发复评：** 用户授权把同8条内部公共对话再次发送到Ollama Cloud
`gpt-oss:120b`。与v4完全相同的48案例在52次请求中执行，46项严格有效、2项最终技术失败；
尝试级原因为46 valid、4 incomplete-response、2 evidence-count，有效输出25 clear、21
violation，审计摘要`43d5ee470aa6d5f13c23ab46a5a642d01a9254e6cf9eaef6c862f47ed8ddaad2`。
与冻结的单一Codex专家诊断一致29/46（63.0%），低于旧v4的31/46（67.4%）；两轮共同有效的
44项中由30项一致降至27项，下降6.8个百分点。epistemic与interaction改善，但role-strategy与
temporal退步，逐项理由显示既有跨维度误用，也有日期冲突漏检。该批已参与规则设计，不能继续
调参追分；结果冻结为开发诊断，不构成人类准确率或架构效果。累计真实开发调用由268增至320次。

**2026-09-12 条件中立语义评分契约：** 单一专家审阅暴露评分器对模拟回执、等义重复、
无效交接所有者和开放前置条件的系统性歧义。现明确：公开发言只证明说过这句话；附件、上传、
外部动作、实时看板与checksum等世界事实须有权威状态或`simulation_executor`结构化成功回执；
合理的ID、链接、哈希或他人确认不能替代回执。等义问题按整个转录累计；未注册/不在场所有者
不能完成交接；开放前置条件或定向义务阻止闭环，状态反转须显式证据协调。契约同等作用于四组、
禁止推断条件，也不把无证据声明直接判为虚假。canonical摘要为
`ed9217e91cd502acabc27ae0c72f64b9496aebab5d5462af1bca06c78e6774f7`；定向24项及完整
SQLite/PostgreSQL 369项验收通过，成功收据摘要
`46ec31a16b6ec98d8867ea64221d97e44dc7a6778ae936658acde5099847a4bf`。
旧v4预测、Codex标签及失败回执均未改写，且旧冻结评分计划不能静默套用新协议。本地修改没有
外部模型调用、推送、部署或线上批次，不构成RoomMind优于Baseline的证据。

**2026-09-12 独立参考标签盲评预检：** 在v4把机械证据定位有效率提高到46/48后，停止继续
迭代模型输出协议，转向缺失的独立人类参考标签。新增两名独立评审者的不同随机顺序与别名包，
各含同一48案例；包内不含来源运行ID、来源条件或v1–v4模型预测。评审者先独立给出
`clear/violation/uncertain/not_applicable`、不可变证据ID及理由，再记录条件猜测与置信度，
用于测量残余盲法；仅分歧项交给不同的第三人裁决，原始标签永久保留。首版响应模板包含严格
导入器会拒绝的额外字段，发现于分发前，作为失败历史保留且从未外发；修正版v2清单摘要
`6f3ccafb750c921bd7874e3af2123c932b58c7c0d2e36b3f8105dabc7b287adb`。完整
SQLite/PostgreSQL验收366项通过，摘要
`f8f474e3caba2dc662272502eee535b4a701233ddff1b03ad7bb06fc4e9fdc07`。当前没有评审者被
指派、没有收集任何人类标签、没有对外分发，也未新增模型调用；分发仍需单独授权和真实评审渠道。

**2026-09-12 Codex单一专家开发审阅：** 用户决定不分发真人双盲包，要求Codex直接判断。
Codex在不读取协调器条件映射的情况下通读8条公开对话，完成6维×8条共48项标签，并为每项
保留不可变证据ID和理由；标签为19 clear、26 violation、3 uncertain，内部摘要
`c9e677efc8ccbebb177b01af3fbc87d6c6ab4a4d32d8a708b841126725345624`。冻结后才连接条件
和v4输出：可比较46项中一致31项（67.4%），角色策略8/8一致，认知/证据忠实度仅2/8一致。
单一专家标签中Baseline为9 clear/14 violation/1 uncertain，RoomMind为10/12/2；分家族方向
不一致，不能声称RoomMind获胜。该结果明确登记为单一AI开发诊断，不是人类标签、双评审、
评分器准确率或确认研究证据；本阶段没有外发、模型调用、推送或部署。

**2026-09-12 证据目录协议v4本地冻结：** 针对v3的40次quote-missing主故障，可信本地
程序先把每条消息切成有源字符范围的有序片段并分配不可变ID；模型以后只返回ID与相关性，
本地映射message、speaker、quote及Unicode偏移。仍使用完全相同的48案例，不按v3结果筛选。
输入摘要`55f0de1fb8f61691030b7055fd21b6dfe92267c6bba877279fe1a6f82f7edc9b`，
最大请求71,094字节。完整SQLite/PostgreSQL验收362项通过，摘要
`128660f06b55ba0fe01b6bfd27952f76eb0954cb1e775ee760b0cca1ee643996`。
首次沙箱内验收因本机socket访问被拒留下失败回执，权限外按原测试复跑通过，未覆盖历史。
用户随后明确授权v4外发；48任务以53请求完成，46项严格有效、2项最终技术失败，较v3
28/48进一步改善。尝试级原因是46 valid、6 evidence-count和1 unknown-evidence-ID；后者
重试成功，两个最终失败均为interaction_structure_fidelity且返回6–7条证据、超过冻结上限5。
完成输出为30 clear、16 violation，审计摘要
`de61866a82e743bb10649d30e8afa4cfc7638a6042578b94676abf7b384a8ac7`；累计调用268次。
不构成准确率、G5效果、部署或确认研究证据。

**2026-09-12 证据绑定协议v2与v3修正：** 用户明确授权同一8条旧公共对话再次外发；
v2的48任务共90请求，严格校验仅10项完成、38项技术失败。尝试级失败包含37次响应不完整、
39次quote/offset不符、2次speaker不符和2次证据数不符；10项有效输出为7 violation、
2 clear、1 abstain，但没有参考标签，不能估计准确率或G5效果。审计摘要
`3ced1eaeb2cf4251751384bc5917df9f3f9b6ebb896c2ea496c59f70c706948a`。
其中25个失败尝试（覆盖19案例，含18个最终失败案例）的精确quote可由程序唯一定位，故新增
v3：模型只给message ID与原文quote，speaker及Unicode偏移由可信本地代码推导。v3同48任务
输入已冻结，摘要`e57b58d366cb4d89ae1afab2e87597c4d1679fe4f4ef7d94584c109fe4c16d71`；
全量358项通过。用户随后明确授权v3外发；48任务以74请求全部执行，28项严格完成、
20项最终技术失败，较v2的10/48有效率明显改善。尝试级结果为28 valid、40 quote missing、
5 evidence-count和1 unknown-message；完成输出为15 clear、10 violation、3 abstain。
审计摘要`8b53c81cb7d20dfa19b58154a31757eee163a9cb1ea1a04885830ebeda4c3d44`。
累计真实开发调用215次。精确quote复制仍不可靠；结果仍无参考准确率或G5效果含义。

**2026-09-12 首批真实开发预测：** 用户授权Ollama Cloud及仅内存复用凭据后，
48旧文本任务通过51请求完成，3技术失败原文保留后重试；请求/HTTP/解析一致性审计通过。
定点核实1处评审理由的说话人归属错误，不能直接把分类输出当真实错误率。
见 `docs/G5_DEVELOPMENT_CALIBRATION_FIRST_REVIEW.md` 和
`research/experiments/2026-09-12-g5-development-calibration-predictions/`。
不是G5四组生成，不证明架构效果；无真人参考、推送或部署。

**2026-09-12 补取来源绑定交接：** 新独立登记保留20旧事件并追加18条开发暴露，
74下载文件建立证据索引；8个slug/4关联组仍排除确认。原194研究文件校验通过。
无新语义资格结论、无模型调用。具体下一授权和W0–W8差距见
`docs/G5_CALIBRATION_HANDOFF_20260912.md`；不以旧归档授权启动真实校准。

**2026-09-12 授权只读历史补取：** 17原归档+18批54当前导出共71文件摘要匹配；
144运行原转录摘要匹配，6组旧/新转录一致。无效G4.2另存3文件audit-only，不恢复。
证据在 `2026-09-12-g5-legacy-server-recovery`。文档标识旧资格批次缺口补齐，
但不认证所有本地压力/试运行历史或重新评价旧结果；无部署/重启/模型调用。

**2026-09-12 已知暴露执行登记：** 新独立SQLite登记连接8个slug为4个父变体组，
绑定4份G4.16输入摘要及8条明确assistant来源的开发暴露声明；20事件重连/重放一致，
所有已知组均不具未见确认资格。原历史/冻结表不变。G2/G3/G4.0–4.5原导出仍待定位，
有限本地搜索不能证明数据丢失。证据在 `docs/G5_LEGACY_REGISTRY_20260912/`。

**2026-09-12 历史库存与暴露核对：** 16份本地导出128运行，126转录摘要匹配，2早期
失败运行缺原摘要；69历史文件和63文档留摘要。四类重复开发场景及world-v3变体保守
排除G5未见确认集。G2/G3/G4.0–4.5原导出位置尚未核实，不声称全历史完整。
见 `docs/G5_LEGACY_EXPOSURE_RECONCILIATION.md`；旧结论/评分/配置未改写，无线上动作。

**2026-09-12 运行交接检查：** 新只读命令验证当前完整G5源码与验收回执，冻结当前
research目录116文件库存；新增运行/恢复说明。350项稳定回归通过，无失败/错误/跳过，
HANDOFF回执摘要 `aa19a49f24cdf4fa1f9066b4a2d23f243a823d685cec4493cf99dd9749d09db7`。
交接包 `docs/G5_LOCAL_HANDOFF_20260912.json`，一致性不等于签名/历史完整性/上线授权。
W0/W7新增工程证据；W8未启动，实际历史材料清点及真实研究门仍待完成。

**2026-09-12 离线归档配置装配：** `assembly.py` 从冻结规格重建受控组件类型，逐层
核验提示摘要、协议、预算和传输声明；只允许显式offline传输注册，不读凭据或动态导入。
接入LocalBatch后，四组28事件与旧脚本入口完全一致，三事件暂停恢复和封存复用通过。
全量345项通过，无失败/错误/跳过，源码稳定；回执ASSEMBLY摘要
`385db73427dd48e60e0bc70b38427e2df68bb9aaf2aa57dc8173c07cf7c90fb1`。
记录在 `2026-09-12-g5-local-assembly`；可信传输不是网络沙箱，未启动线上研究。
下一包收拢运行发布检查与可执行交接要求；真实校准、历史清点和研究质量门仍未完成。

**2026-09-12 本地冻结批次入口：** 新合成批次控制连接预检、全部分配配置、持久单实例
锁、预定重开和源封存；四组28事件/断点续跑已留档。真实子进程提交前后恢复、本机
PostgreSQL重连与SQLite事件一致性均有验证。工件 `2026-09-12-g5-local-batch-entry`。
仅可信脚本入口，不是部署/外部模型授权或真实效果证据；下一包补归档配置装配器。
最终341项稳定回归通过，无失败/错误/跳过；回执BATCH_ENTRY_FAIRNESS，先前测试环境
失败和340项成功回执均保留。验证进程已退出，未启动线上或外部评价。

**2026-09-12 分析目标与设计诊断：** v3显式区分固定基准观察值与独立家族总体，前者
不输出总体/未来运行区间，v1/v2不改写。验证家族等权、相关重复不增独立n、缺失上下界
与N/A未定义；复用旧稀有家族bootstrap反例，新增18项联合有界区间控制及破坏独立性
的精确反例。工件 `2026-09-12-g5-local-design-analysis`，不构成真实效果或覆盖率认证。
全量334项通过，源码稳定且无失败/错误/跳过；回执
`docs/G5_LOCAL_ACCEPTANCE_20260912_DESIGN_ANALYSIS.json`，验证进程已退出。

**2026-09-12 启动前契约审计：** 只读preflight绑定研究/分析/样本规格/家族登记快照和
原始校准证据，拒绝过期、家族合并、既往暴露/校准重用、缺失参考与预测、样本不符等。
合成工件 `2026-09-12-g5-local-study-preflight`。通过仅代表本地契约检查，研究质量、
真人来源认证、正式n与评分器验证仍未完成，始终不授权启动；旧运行器未改写。
完整回归326项通过，无失败/错误/跳过，源码稳定，回执
`docs/G5_LOCAL_ACCEPTANCE_20260912_STUDY_PREFLIGHT.json`；所有验证进程已退出。

**2026-09-12 材料与家族暴露登记：** 增加只追加登记、亲缘/精确快照关联传播、快照过期
拒绝和暴露先落盘的审阅准备。旧24例合成材料复用但不重新赋真人标签，全部历史输入保留。
工件 `2026-09-12-g5-local-material-intake`；不能识别未登记语义改写或证明历史暴露完整性，
也不构成正式研究启动许可。下一包将此登记绑定研究启动前门禁。
全量319项通过，无失败/错误/跳过，源码稳定；独立回执
`docs/G5_LOCAL_ACCEPTANCE_20260912_MATERIAL_INTAKE.json`，验证进程已结束。

**2026-09-12 报告契约v2：** 条件错误率之外增加明确参考层未决预测界，未知参考不造真值；
分析v2标实际区间方法，绑定新manifest才运行，旧v1工件/哈希保持。旧24例校准和评分
控制计划重算一致，新四组评价含技术失败/重连及完整源校验。工件在
`2026-09-12-g5-local-report-contracts`，不是实际效果。全量312项通过，100.042秒，源码
稳定且无失败/跳过；回执 `docs/G5_LOCAL_ACCEPTANCE_20260912_REPORT_CONTRACTS.json`。
验证进程结束，下一完整本地包为校准材料/家族暴露登记和审阅准备，整体研究仍未完成。

**2026-09-12 研究协议草案：** 完整交付先导/筛选/确认分流、两轨公平对照、建议主要问题、
六维锚点和正负例校准规格、家族/缺失/停止与冻结规则。决策登记保留真实未定参数null，
不把旧数据或示例alpha/δ当正式设计。说明性Hoeffding精度计算可重算，但不是功效保证。
草案 `docs/G5_RESEARCH_PROTOCOL_DRAFT.md` 引用公开原始研究；无内部数据外发或模型调用。
下一完整本地包补齐报告的未决分母界和分析元数据版本一致性；整体发布与真实研究仍未完成。

草案/规划工具包集中回归306项通过，100.377秒，源码前后稳定，无失败/跳过；
回执 `docs/G5_LOCAL_ACCEPTANCE_20260912_RESEARCH_PLANNING.json`，验证进程已结束。

**2026-09-12 本机预测执行所有权：** 新local_worker内核锁接入预测run_missing/公开append，
同库合作执行者不能并发恢复或在模型等待时抢写；锁持续到流关闭，取消请求不等于释放。
真实子进程提交前/后强制终止恢复保留前缀，竞争者0调用，恢复分别4/3次脚本推理。
工件在 `2026-09-12-g5-local-worker-recovery`，原始数据/协议不迁移，不追溯认证旧worker。
仅本地Unix合作式范围，不是分布式租约、远端恰好一次或实际服务部署；全部发布门仍未完成。
下一工作包转W6具体研究协议/校准规格，不以零散工程测试代替研究计划。

本机所有权包集中回归302项全部通过，170.085秒，源码前后稳定、无失败或跳过，
回执 `docs/G5_LOCAL_ACCEPTANCE_20260912_WORKER_OWNERSHIP.json`。全部测试进程退出；G5整体仍未完成。

**2026-09-12 发布差异/规模审查：** 复用已封存4/24例合成参考工件，确认预测日志
导出及新增事件同一事务内重复全量验证，现改为一次；仍逐事务验证全部历史/来源/回执。
三种负载输出哈希完全一致，恢复参数严格布尔，重算链哈希也不能绕过语义校验。
新目录 `2026-09-12-g5-local-release-scale` 保留修复前后测量，时间不是吞吐保证。
逐门审查明确：单worker没有租约/旧进程停止证明，真实质量、研究计划与部署仍未完成。
下一完整本地包为可靠单实例执行与安全恢复入口；不自动外发、推送或部署。

发布审查包最终回归294项全部通过，241.325秒，源码前后稳定；新回执
`docs/G5_LOCAL_ACCEPTANCE_20260912_RELEASE_SCALE.json`。工件已保存，进程退出，整体发布门仍未完成。

**2026-09-12 固定预测器/事务日志：** 新model_spec绑定提示/模型/请求与原始回执，
分类器不见参考标签；独立SQLite保存started/finished，技术失败可追加重试，完成不能
覆盖。中断调用保留未确定，显式恢复须确认旧worker停止，不保证远端exactly-once。
真实进程回滚/确认丢失及脚本推理通过，4源4例完整工件留在
`2026-09-12-g5-local-prediction-ledger`，旧记录不追溯认证。全量回归另存新回执；
下一完整本地包转W7发布差异/规模审查。无外部推理、推送、部署，真实研究门尚未完成。

固定预测包最终回归292项通过（PostgreSQL 47），125.729秒，前后源码指纹一致，
无失败/跳过；新回执 `docs/G5_LOCAL_ACCEPTANCE_20260912_PREDICTION_LEDGER.json`。
完整包已结束并保留工件，全部发布门尚未完成，不以脚本预测准确率当真实结论。

**2026-09-12 案例单位校准桥接：** 源/标注/裁决重验后按冻结案例而非label记录计算，
保留参考未决和预测缺失/失败/弃权交叉分母；技术失败重试显式引用，完成不可覆盖。
24例/4源的完整合成工件存于 `2026-09-12-g5-local-calibration-bridge`，只有10明确配对，
不能把脚本FP/FN为0解释为真实系统无误报。旧协议/旧数据不改，未外发/部署/推送。
真实请求回执适配和预测执行持久化为下一本地包，真实效果和最终发布门仍未完成。

案例桥接最终回归287项通过（PostgreSQL 47），146.325秒，稳定源码、无错误或跳过；
新回执 `docs/G5_LOCAL_ACCEPTANCE_20260912_CALIBRATION_BRIDGE.json`。本只读桥接工程
包完成，无新增外部动作；此前所有工件保留，下一包续办点和完整清单已更新。

**2026-09-11 独立标注/裁决审计：** 新源绑定不可变归档保留独立原标签、精确引文、
补充工件与裁决全部引用；缺失/分歧不自动变为明确标签，裁决不能覆盖原记录。身份来源
显式但不认证真人/独立性。空转录明确保留uncertain/N-A及旧格式导出限制。全合成
24案例/56记录与4个源保存在 `2026-09-11-g5-local-annotation-audit`，无真人/外部模型。
SQLite真实进程回滚/确认丢失及重连通过；完整回归另存新回执。W6工程推进，实际标签/
模型校准仍未完成，后续以案例而不是标注记录为单位桥接结果。全部旧工件原样保留。

独立标注包最终验收282项通过（PostgreSQL 47），82.578秒，无错误/跳过，源码指纹
一致；回执 `docs/G5_LOCAL_ACCEPTANCE_20260911_ANNOTATION_AUDIT.json`。工程包完成，
真实标签/模型校准与全部发布门未完成，下一本地包按案例单位桥接校准结果。

**2026-09-11 冻结评分控制：** 新分析计划显式绑定无损JSON呈现、正反顺序和重复次数；
每格独立不可变归档，面板中断只补缺失/技术失败评分，不覆盖已完结果。源文本/引文
位置不改，原始请求/响应与控制身份可验，重复评分不当新增样本。新本地合成工件包含
4个源、8格和192次评分，能检出预先注入格式差1分；不代表真实judge无偏或语义风格
校准完成。工件保存于 `2026-09-11-g5-local-evaluation-controls`，全量验收另写稳定源码
回执。W6真实标签/语义风格/研究假设与W7最终门未完成；无外部调用、推送或部署。

评分控制包最终回归276项通过（PostgreSQL 47），114.321秒，无失败/跳过，前后指纹
一致；回执 `docs/G5_LOCAL_ACCEPTANCE_20260911_EVALUATION_CONTROLS.json`。
本工程包完成；独立标注/分歧裁决契约为下一完整本地包，未将合成测试当效果结论。

**2026-09-11 无损认知增量存储：** 显式冻结新存储协议，保留旧快照和所有原始证据；
模型仍消费完整状态，仅审计采用字典/列表前缀差量及周期完整检查点。世界/角色/前置事件
和前后状态哈希验证贯穿读取、双数据库事务提交、冻结及离线源校验。两库四组全组件
重连哈希一致、事务内进程退出回滚与提交后回执恢复通过。新四组40轮/12次重开工件留存在
`research/experiments/2026-09-11-g5-local-delta-storage/`：B/D实际认知字段比同次运行逐轮
全量状态少73.09%，每轮精确重建与实际输出一致。不是整体数据库/吞吐/行为效果结论。
稳定源码集中验收271项通过（PostgreSQL 47），回执
`docs/G5_LOCAL_ACCEPTANCE_20260911_DELTA_STORAGE.json`；无失败/跳过，前后指纹一致。
本包完成；W4真实语义校准、W6风格/顺序/重复评价控制及W7最终门仍未完成。没有外部
调用、推送、部署、旧数据迁移或删除。完整清单和下一包续办点见G5主文档。

**2026-09-11 完整请求容量与长程观测：** 共同冻结字节预算覆盖生成、反思、规划、
治理、标注和向量请求；超限不截断，单独留痕，原始正文不进入元数据。双数据库四组
重连验证通过，40 轮/12 次重开夹具及源库全文均保留在新的 g5-local-capacity 目录。
输入峰值 9,524 字节，但 B/D 源快照约 4.2 MB，暴露完整认知审计的存储增长；后续需
增量存储/精确重建，不能宣称无限长程已解决。原生 HTTP 包装递归兼容问题已修复并用
本地 mock 验证；非稳定源码的第一次回归回执保留且不当作发布依据，最终回执另存。
无外部模型调用、推送、部署、旧数据修改或研究目标改变。W0–W8 与精确剩余门见主文档。

容量包最终验收 **252 G5 + 9 契约 = 261 项通过**（PostgreSQL 45），340.855 秒，无跳过，
源码/测试前后指纹一致，编译与差异空白检查通过。最终回执单独保存为
`docs/G5_LOCAL_ACCEPTANCE_20260911_REQUEST_CAPACITY_FINAL.json`；之前源码变更中的
非最终回执保留，不混用为发布证据。完整请求容量工程门完成，存储增量/精确重建以及
真实评价校准、最终发布门仍未完成；下一工作包和历史保护要求已记录。

**2026-09-11 源证据闭环与统计压力诊断：** 完整源快照现可在双数据库事务中导出，
离线重算事件、模拟回执与问答/会话状态，再与全部评分输入和原始评分工件交叉核对。
全组件四组适配器不可用恢复与未中断事件对照通过，不重启用户数据库。新增离线证据
CLI 与带源码指纹的集中验收回执工具；不因此宣称已满足所有发布门。
继续统计压力检查发现十家族 bootstrap 在稀少家族效应下严重欠覆盖的解析反例；原始
合成数据与保守界限比较分别留存，新增显式冻结的 bounded-family-hoeffding-v1，旧
plan 不改写。新界限在小样本很宽，不是假装解决精度问题；实际假设/样本量和评价者
校准仍待完成。全部清单和续办点已追加 G5 主文档，仍无外发、推送、部署或旧实验重启。

本次最终集中验收 **244 G5 + 9 契约 = 253 项通过**（PostgreSQL 44），无跳过，292.408 秒，
源码/测试前后指纹一致，编译和差异空白检查通过。回执保存在
`docs/G5_LOCAL_ACCEPTANCE_20260911_SOURCE_RELEASE.json`，两个覆盖压力 JSON 分别留存；
旧回执不覆盖。W4/W5/W6/W7 仍未整体完成，具体剩余门见 G5 清单，不以工程测试替代真实
行为优势、评分者校准或正式研究样本。

**2026-09-11 共同观察→源密封→独立评分闭环：** 四组共同观察窗口显式冻结，C1 只保留
实际交付的信息；源对话最终密封在 SQLite/Postgres 持久化，禁止密封后公共提交。
独立六维适配只用内部密封原文与共同评审真值；本地评分归档要求全部分配密封，保留
原始请求/响应及失败，断点续评只补缺维度，完成/N/A 不覆盖。事务中及提交后真实进程
退出恢复通过。完整 G5 233 + 后补进程测试 1 + 契约 9 = **243 项通过**（Postgres 42），
编译/差异空白检查通过；较前一已记录 223 项增加 20 项工程验收，不代表总体完成率。
W4/W5/W6/W7 整体门仍未完成；真实语义校准、区间覆盖、全提示容量和集中发布留在清单。
全部旧记录保留；未推送、部署或调用真实外部模型。完整交付、限制及精确续办点见 G5
主文档末节，内部评审真值不得当公开工件传播。

**2026-09-11 完整工作包推进：** W3 可靠重开请求闭环已完成双数据库/四组哈希对照及
提交前、事务内、提交后真实子进程退出验收。v2 绑定请求 ID、episode 和版本；成功事件与
回执同事务，拒绝/迟到回执持久化，技术失败留痕；旧协议不重写。随后连续实现 W4 来源
层级计划、创建后完成证据与跨会话持续性、120 条干扰验收，以及 W6 六维证据/来源账本、
完整分母、家族配对效应和冻结分析接口。最近全量 **213 G5 + 9 契约 = 222 项通过**，
其中 Postgres 39 项；不是语义效果结论。W4 长程容量、W6 盲评适配/真实校准、W7 发布门
仍未完成，不能称只剩授权。实现及剩余门见 `docs/G5_ARCHITECTURE_AND_STUDY_REDESIGN.md`
与 `docs/G5_MEASUREMENT_AND_RELEASE_GATES.md`。无推送、部署、外发或旧历史改写。

同次连续实施还补齐增量计划与终态正文归档：完整任务/解析更新仍保存，模型只见开放任务
与必要依赖摘要；超过冻结活跃容量明确失败，不删未解决意图。修复终态转换后来源上下文
重建不一致，最终 **214 G5 + 9 契约 = 223 项通过**（Postgres 39，无跳过），编译与差异
空白检查通过。W4 共同公共观察窗口仍待冻结与公平性验收；W6 盲评适配及真实校准、W7
集中发布门未完成。下一精确断点已写入 G5 主文档，不能据这些合成测试宣布行为优势。

**2026-09-11 执行方式调整：** 用户要求纠正零碎补测推进。续办任务已改为完整工作包
设计→实现→验收，优先 W3 请求可靠性收尾，再 W4 长期认知、W6 测量、W7 集中发布验收。
小测试增量不再作为独立里程碑，不以测试数量估计总进度；原四十九阶段全部保留。
本次不新增功能完成声明，基线仍 188 项通过；授权与历史保护边界不变。

**第四十九阶段更新：** 全组件重开 Postgres 四组、关闭重连与 SQLite 对照通过；已打开
状态重复请求不追加，不等于稳定请求幂等返回。回归 **187 → 188 项通过**，Postgres 28 项。
确认丢失/迟到请求与失败恢复待补，仅本地模拟，无部署或历史改写。

**第四十八阶段更新：** 全部当前适配器＋调度 SQLite 四组显式重开并再次关闭通过，
下一原定角色发言、旧历史与待答延续。完整回归 **187 项通过**，比上轮分批覆盖增加 1 项，
含 Postgres 27 项。重开数据库/恢复专项待验证，仅本地模拟，无部署或历史改写。

**第四十七阶段更新：** 补跑 G5 全量 176 项（含 Postgres 27）通过，追加预算测试后
session_runtime 12 项及契约 9 项通过，合计覆盖 186 个不同测试。新增重开治理耗尽/漂移
与预算拒绝边界，上轮数据库缺口已补；不是最后一次单条全量 186 项。无部署或历史改写。

**第四十六阶段更新：** 新增冻结开关的显式重开机会，由下一原定角色生成并经治理/标注，
不愿重开不发布，总预算不重置。新增 2 项 SQLite 测试；本轮实际 **148 G5＋9 契约通过，
27 Postgres 跳过**。全量候选 184，不能称全量通过；两次权限审核超时阻止数据库回归执行。
后续补回归/完整重开组合，无部署或历史改写。

**第四十五阶段更新：** 最后关闭标注超时不改变事件/会话/待答，Postgres 重连续跑与
不中断参考一致，失败保留。回归 **181 → 182 项通过**，Postgres 27 项。权限审核首次
超时后一次重试成功；不是实验故障。显式重开待实现，无部署或历史改写。

**第四十四阶段更新：** 全组件显式关闭 Postgres 四组与 SQLite 一致；关闭重连后无新
调用、事件或尝试，待答保留。回归 **180 → 181 项通过**，Postgres 26 项。仅本地模拟及
连接池重连，显式重开和长期机制仍未完成，无部署或历史改写。

**第四十三阶段更新：** 全组件显式结束话语＋模型标注＋优先调度 SQLite 四组通过，
待答不清空，重建后不继续生成，不推断成功。回归 **179 → 180 项通过**。
仅脚本夹具，关闭数据库专项及显式重开待完成，无部署或历史改写。

**第四十二阶段更新：** 全组件＋优先调度＋会话 null 标注 Postgres 四组与 SQLite 一致，
会话标注超时后重连续跑不改事件、保留失败。回归 **177 → 179 项通过**，Postgres 25 项。
仅连接池重连/本地模拟，完整关闭/重开和真实语义验收尚未完成，无部署或历史改写。

**第四十一阶段更新：** 全部当前适配器＋优先调度＋会话 null 标注 SQLite 四组通过；
D 会话标注超时后重建续跑与不中断事件一致，失败保留。回归 **175 → 177 项通过**。
不等于完整关闭/重开或真实语义验收，新组合数据库专项待补，无部署或历史改写。

**第四十阶段更新：** 模型会话标注 Postgres 四条件完整事件与 SQLite 一致，回执私有且
关闭重连后不再调用。回归 **174 → 175 项通过**，Postgres 23 项。仅模拟 transport 与
连接池重连，显式重开/全部组件组合仍待完成，无部署或历史改写。

**第三十九阶段更新：** 优先调度＋问题＋会话停止 SQLite 四组组合通过，重复本人结束
意愿不代替他人，重建后基础机会不丢且待答保留。回归 **173 → 174 项通过**，Postgres 22 项。
仅脚本/对象重建，完整模型和数据库组合及重开入口待完成，无部署或历史改写。

**第三十八阶段更新：** 模型会话标注 SQLite 四条件回执保存/输入与公开隔离通过，
错误回执不发布、失败记录保留且续跑仅提交一次。回归 **171 → 173 项通过**，Postgres 22 项。
仅本地模拟，新模型数据库专项及重开/完整调度待完成，无部署或历史改写。

**第三十七阶段更新：** 新增固定模型会话标注适配，严格回执/JSON/片段约束，null 也保留
取证；Runtime 增加私有证据解包，组合专项待补。回归 **168 → 171 项通过**，Postgres 22 项。
仅模拟响应，不证明语义准确率，无外部调用、部署或历史改写。

**第三十六阶段更新：** 新会话停止 Runtime 的 Postgres 四条件与 SQLite 事件/投影/视图
一致，关闭后重连及重建不新增调用、事件或尝试。回归 **167 → 168 项通过**，Postgres 22 项。
仅本机脚本与连接池重连验证；显式重开及完整调度组合待完成，无部署或历史改写。

**第三十五阶段更新：** SQLite 四条件关闭保留待答，C/D 修订仅标注最终话语，D 耗尽
等待不触发会话标注/关闭。回归 **165 → 167 项通过**，含原有 Postgres 21 项。
新 Runtime 数据库/调度组合与重开入口仍待完成；仅脚本验收，无部署或历史改写。

**第三十四阶段更新：** 共同冻结会话标注与停止协议接入 Runtime，全员结束后无新角色调用，
不推断任务成功；标注失败不发布，缺失/漂移拒绝。SQLite 四组与恢复验证通过，回归
**162 → 165 项通过**。新运行时组合/Postgres 专项及显式重开入口待补，不证明语义效果，无部署。

**第三十三阶段更新：** 问题/会话组合入口 Postgres 与 SQLite 完整事件、双投影及角色视图
一致，重连保持待答，任一无效标注不部分提交，有效重开/延期幂等。回归 **161 → 162 项通过**，
Postgres 21 项。Runtime 生命周期仍待接入；仅本地脚本验证，无部署或历史改写。

**第三十二阶段更新：** 会话与问题标注统一到一次事件提交，任一无效整体不发布；
SQLite 验证结束不清空待答、重开可同时延期原问题且幂等。全量回归 **159 → 161 项通过**，
含原有 Postgres 20 项。新组合 Postgres/Runtime 接入待完成，不证明语义效果；无部署或历史改写。

**第三十一阶段更新：** SessionJournal Postgres 关闭/重连/重开与 SQLite 一致，并发重开
仅提交一次，无效请求不落库。完整回归 **157 → 159 项通过**，Postgres 20 项。
尚未接 Runtime 与问题日志组合，不等于完整停止机制；无部署或历史改写。

**第三十阶段更新：** 显式 SessionJournal 协议将结束/重开标注与话语原子保存，SQLite
重开恢复、幂等及无效请求隔离通过。完整回归 **154 → 157 项通过**。
尚未接 Runtime 或验证新入口 Postgres 组合，不是全局停止锁；无部署或历史改写。

**第二十九阶段更新：** 新增显式结束意愿/重开纯投影，结束不等于任务成功，公开继续讨论
清除旧意愿，重开保留历史。完整回归 **150 → 154 项通过**。保守候选规则尚未接运行时，
不覆盖所有离场/终止方式，不证明语义准确性；无部署或历史改写。

**第二十八阶段更新：** SQLite 三角色多目标四组验证通过：等待不无限优先，基础机会不丢，
延期和拒绝保留且不伪记完成，运行时重建后投影不变。完整回归 **148 → 150 项通过**。
仅十轮脚本调度测试，不是长期行为或模型质量证据，无部署或历史改写。

**第二十七阶段更新：** 新调度双数据库四组×开关一致，Postgres 优先机会提交前/后客户端
退出恢复不丢失或重复机会。完整回归 **145 → 148 项通过**，Postgres 18 项。
仅双角色模拟及客户端退出验证，不代表真实行为效果，无部署或历史改写。

**第二十六阶段更新：** 共享调度规格与四组哈希绑定，优先机会接 Runtime 并随事件原子保存；
SQLite 四组开关、失败不耗机会和配置漂移测试通过。完整回归 **141 → 145 项通过**。
默认旧路径不变；新调度 Postgres/进程专项待验证，无部署或外部调用。

**第二十五阶段更新：** 新增可关闭的有界问题优先机会选择器，目标有序、延期不催答、
每目标一次优先、基础游标不被插队跳过；尚未接入 Runtime/冻结清单。完整回归 **136 → 141 项通过**。
仅本地机制测试，不代表行为效果；现有四组默认调度及历史不变，无部署。

**第二十四阶段更新：** Postgres D 组客户端子进程提交前/提交后确认返回前突然退出恢复通过，
不丢轮或重复发布，started 历史保留。完整回归 **134 → 136 项通过**，Postgres 15 项。
仅本地模拟和两个客户端故障边界；未重启数据库服务、部署或改写历史。

**第二十三阶段更新：** Postgres C/D 组修订成功、耗尽和超时重试六种组合与 SQLite
事件/视图/调用/问题投影一致；草稿隔离与失败历史保留。完整回归 **133 → 134 项通过**，
Postgres 13 项。仅本地模拟验证，无生产逻辑变更、部署或历史改写。

**第二十二阶段更新：** SQLite D 组实际子进程在提交前及提交后确认返回前突然退出，
恢复不丢失或重复轮次，保留 started 历史；事件/问题/角色视图与不中断参考一致。
完整回归 **131 → 133 项通过**。仅模拟响应和两个客户端退出边界，不是数据库宕机验证，无部署。

**第二十一阶段更新：** SQLite 组合验证覆盖同角色修订、拒绝草稿私有隔离、修订耗尽静默
未解决，以及修订超时失败留痕后续跑一致。完整回归 **128 → 131 项通过**，Postgres 仍为 12 项。
仅脚本审查与响应，不是语义质量验证；本轮未改生产逻辑或历史数据，无部署。

**第二十阶段更新：** Postgres 四条件当前适配器组合与 SQLite 逐步事件、角色视图、请求轨迹
和问题投影一致；D 组标注失败后重连保留失败历史并恢复同一结果。完整回归 **126 → 128 项通过**，
Postgres 12 项。仅模拟模型和连接池重开，W7 尚未完成，无部署或历史数据改写。

**第十九阶段更新：** 四条件当前模型适配器全组合通过 SQLite 轨迹测试；D 组标注超时后
数据库重开续跑与不中断参考事件/问题投影一致，失败历史保留。完整回归 **124 → 126 项通过**。
仅脚本响应和连接重开，不是模型效果或进程崩溃验证；W7 未全部完成，无外部调用或部署。

**第十八阶段更新：** 固定模型问题标注适配完成协议验证，模型证据留在私有事件审计；
新增 Postgres Runtime 提问/延期回应组合测试。完整回归 **120 → 124 项通过**，
其中 Postgres 10 项。仅本地模拟，真实语义质量及队列驱动调度尚未验证，无外部调用或部署。

**第十七阶段更新：** 共享冻结的问题标注规格接入 Runtime，最终话语标注与发布原子保存，
失败留痕且不推进轮次；队列仅作审计，不改变四条件调度。完整回归 **116 → 120 项通过**。
语义标注与队列驱动调度未完成，历史与线上路径不变。

**第十六阶段更新：** 新问题日志通过 Postgres 专项验证，包括双数据库一致性、
连接池重连、并发幂等和数据库失败原子回滚。完整回归 **113 → 116 项通过**，
其中 Postgres 由 6 项增至 9 项。仍未接入 Runtime，历史与线上路径不变。

**第十五阶段更新：** 问题标注借助既有事件日志原子保存并重放恢复，显式协议启用。
新增 SQLite 重开/幂等/失败隔离测试，完整回归 **109 → 113 项通过**。
尚未接入 Runtime 或单独验证新封装的 Postgres 链路，历史数据不变。

**第十四阶段更新：** 多受话人问题投影支持确定重放和逐人回应历史，延期不丢失，
拒绝不计同意，禁止角色替代与终态覆盖。完整回归 **104 → 109 项通过**。
当前为隔离纯函数及脚本标注，尚未接入持久化/调度，旧数据与运行路径不变。

**第十三阶段更新：** 校准样本冻结与分层描述统计接入；缺失、技术失败、弃权、
不适用分开计数，误报/漏报明确条件分母，标注来源分层。完整回归 **99 → 104 项通过**。
仅合成数据验证统计逻辑；真实标注和校准尚未开展，历史数据保留。

**第十二阶段更新：** 固定模型审查适配接入独立治理，校验请求/响应和配置，
审查模型证据留入私有事件审计。完整本地回归 **94 → 99 项通过**。
未调用真实服务；审查准确率和误报校准尚未完成，历史数据与线上路径不变。

**第十一阶段更新：** 独立候选治理契约接入，区分硬发现、建议和不确定发现；
同角色修订，不代写发言，私有保留候选审计。完整本地回归 **88 → 94 项通过**。
审查器仍为脚本注入，尚未验证真实语义判断和误报率，不作为行为改善证据。

**第十阶段更新：** 批量嵌入评分接口接入来源记忆，校验向量与模型回执，冻结评分规格，
保留私有检索证据。新增 5 项合成向量测试，完整回归 **83 → 88 项通过**。
仅本地协议/计算验证，未验证真实语义检索质量；旧数据与线上路径不变。

**第九阶段更新：** 新增固定模型反思/规划生成适配，冻结各自提示及传输规格；
输出仍经来源/角色/操作验证，生成回执随私有状态保存，不进入决策上下文。
失败保留旧状态、配置漂移阻止继续。回归由 77 项增至 **83 项通过**，
全部为本地验证，未进行真实模型调用；语义支持与生成质量尚不能据此认定。

**最新实施记录：** 显式 Ollama HTTP 传输层与 ModelPolicy 已接通，路由哈希及协议配置
进入冻结规格；禁用环境配置发现、重定向与自动重试/切模型，校验响应完成状态和模型。
新增 7 项模拟 HTTP 测试，完整回归从 70 项增至 **77 项通过**（含 6 项隔离 Postgres 测试）。
仅本地验证，没有真实模型调用或发布。详情见 G5 设计第八阶段；真实生成、独立治理和评价仍未完成。

用户授权必要的大幅调整，不以成本限制设计。G4.16 的筛查未展示优势，且整体两组比较
不能归因认知、调度和治理的贡献。因此 G5 分开共同世界、角色认知、基础调度、可切换治理
和外部评价；新增 2×2 因子实验，保留原强基准作为不同的端到端研究轨道。

完整方案和 W0–W8 验收清单见 [G5 重构设计](G5_ARCHITECTURE_AND_STUDY_REDESIGN.md)。
当前只新增离线实验契约及测试，未修改 G4.16 在线运行路径，未推送、部署或启动新批次。
所有历史结果保留原始证据身份；离线校验通过不代表对话改善，助手审计不等同真正人类评审。

**后续实现更新（同日）：** 新增隔离的 `app/g5` 共同世界和四条件共用顺序循环。
本地 SQLite 事件日志实现冻结定义、追加式哈希链、私有视图、精确模拟回执、原子提交与幂等恢复；
四条件仅通过显式认知/治理适配器开关区分。新增 17 个测试通过，连同实验契约共 26 个。
当前为离线参考运行时，不是完整 G5：生产数据库、真实 LLM、问题队列和语义治理尚未接通，
所有待办与未验证项记录在 G5 设计文档。本轮未触及旧运行路径或历史实验文件。

**第三阶段更新（同日）：** 新增异步 Postgres 适配、阶段调用日志与冻结角色卡输入。
本机隔离 schema 集成测试确认并发串行化、幂等、重连、失败保留、原子回滚与 SQLite 语义一致。
提交确认丢失标为不确定，以事件日志决定游标；私有角色设定不传给其他角色。
G5 相关测试累计 40 项通过。测试临时 schema 已清理，未改旧应用数据、未部署、未调用真实 LLM。
这仍是基础设施进展，不表示 G5 已完成或对话质量已提升。

**第四阶段更新（同日）：** 接入固定绑定的决策模型契约与响应证据，过滤角色视图，拒绝模型漂移、
截断/畸形结果，不静默回退。新增 7 项测试，完整相关测试累计 47 项通过（含本机 Postgres）。
外部 transport 尚未接入，认知模块仍待实现。按用户持续推进授权创建 `roommind-g5` 续办任务，
承接本地实现与测试，不自动部署、启动新线上实验或邀请真人评审。

**第五阶段更新（2026-09-10 续办）：** 接入角色/世界作用域绑定的来源记忆、重复观察去重、
历史事实保留、词法检索和可注入语义评分接口。完整记忆仅保留在私有持久状态，决策与治理接收
选中的 context。新增 7 项测试，相关回归累计 54 项通过（含本机 Postgres）。
尚无真实嵌入、反思或规划实现，不声称记忆带来质量收益；未部署或触碰历史数据。

**第六阶段更新（2026-09-10 续办）：** 新增反思/计划组合接口：检索来源引用校验、
假设与事实分离、本人行动权限、证据改变触发重规划和计划历史关联。生成器仍为脚本注入，
不是已完成的模型认知。新增 8 项测试，相关完整回归累计 62 项通过，旧实验保持不变。

**第七阶段更新（2026-09-10 续办）：** 新增严格组件规格绑定，校验完整角色卡、组件开关及
运行中配置漂移；加入无证据冷启动询问/等待/延期/拒绝正例，禁止凭空引用支持断言或执行。
新增 8 项测试，完整相关回归累计 70 项通过。严格声明不等于真实行为证明，线上入口尚未开放。

## G4.15：类型化发布声明与终态阶段治理

**触发证据：** G4.14的4/4 RoomMind运行通过全部自动探针，但AI均值仍落后Baseline 0.938，
人工精读还发现运行550的无依据运营就绪反转、运行552的虚构制品审阅与候选问答阶段重开、
运行554的未注册事故所有者和跨角色代行动作，以及运行548的模板化畸形回退。自动清洁率与
可见语义质量发生明显背离。

**主要机制：** 最终可见发言被归一化为包含发言者、动作、对象、时间、证据来源和权限范围的
`PublicationClaim`；运营动作、就绪断言和当前会话制品审阅必须绑定已接受的模拟工具结果；
未注册公开所有者与跨角色运营代办在发布层拒绝；最终引文落地复检获得与草稿阶段相同的私有
约束、权限、证据和阶段上下文；结构化面试进入`candidate_questions`后仅允许回答候选人问题，
不能重开评估；模板化回退成为独立硬门槛。

**保持不变：** Stanford式独立记忆与认知循环、公共账本与义务图、共享玩家、Baseline、四个
场景、固定模型、seed策略、独立六维评价及冻结哈希协议均不改变。

**在线资格结果：严格失败。** 授权修订
`5752d34bdeb08b9b35cb048011ed9b9e247c68c9`部署后，批次
`ff6098d3-0cc5-41f6-90a2-5e35076c5dd2`完成8/8冻结对话、零对话失败、零降级输出和48/48
独立评价，八条转录哈希均复算一致。但仅运行562通过全部适用探针，其余三条RoomMind运行
均未通过G4.15复合门槛；AI均值RoomMind为4.538、Baseline为5.250，差值-0.713，RoomMind
仅1胜3负。人工精读发现运行556的无依据当前产能与附件声明及受话人错配、运行558的多受话人
响应链断裂、运行560的终态后发言，以及运行562的重复低信息回退。G4.15新增类型化诊断在四条
运行上均为空，仍漏掉部分可见缺陷。未启动外部人工盲评。完整结果见
`docs/EXPERIMENT_G4_15_QUALIFICATION_RESULTS.md`。

## G4.14：发布所有者与实时制品证据收敛

**触发证据：** G4.13 虽将 RoomMind 严格清洁通过率从 0/4 提高到 1/4，但运行 542、544
仍让玩家明确点名的角色静默，运行 546 的 NPC 定向问题仍被玩家中转打断。逐条精读还发现
“已上传并审阅”“路线图已在 Confluence”“日志已复制到归档桶”等无模拟工具依据的实时制品
完成声明，而旧探针没有全部捕获。

**主要机制：**

- 当前玩家消息的结构化 `public_intent.target_id` 与公开文本受话人共同形成发布所有者；
- 必答约束穿透决策、普通发言、计划回退和最终引文落地边界，最后的角色局部答复不提交账本；
- NPC 定向问题允许按在场角色数形成有界响应链，每个目标同轮最多重新调度一次，避免循环；
- 实时制品证据检测覆盖上传后审阅、共享仓库/Confluence 可用、归档桶复制和 manifest 生成；
- 新增 G4.14 复合探针，把发言权、直达路由、管理式路由话术和实时制品证据统一为资格门槛。

**保持不变：** Stanford 式独立记忆与认知循环、公共账本与义务图、共享玩家、Baseline、四个
场景、固定模型、seed 策略、独立六维评价以及冻结哈希协议均不改变。

**在线资格结果：严格失败。** 授权修订
`6b4e461d5d69659bb8546e285b96f77eccfa4539`部署后，批次
`e29005cf-d6e5-4e4b-b4e1-34e5036f1e5e`完成8/8冻结对话、零对话失败、零降级输出和48/48
六维评价；八条转录哈希均独立复算一致。四条RoomMind运行首次全部通过G4.14及适用旧探针，
但AI描述均值RoomMind仅4.521、Baseline为5.458，差值-0.938，RoomMind为0胜1平3负。
人工逐对精读发现探针未捕获的当前世界就绪断言、无依据制品审阅、未注册事故所有者、跨角色
代行动作、候选人问答阶段重开和模板化必答回退。该结果表明清洁探针率从1/4升至4/4，却没有
转化为语义可靠性，属于探针覆盖不足而非资格通过。未启动外部人工盲评。完整结果见
`docs/EXPERIMENT_G4_14_QUALIFICATION_RESULTS.md`。

## G4.13：必答发言权与终态收敛

**触发证据：** G4.12 的四个 RoomMind 运行均未通过严格资格门槛。三个运行出现玩家点名对象未按序回答，产品发布运行在拒绝的礼貌确认后继续开放发言权，访谈运行还出现无工具证据的当前 dashboard 存储断言。

**主要机制：**

- 玩家明确点名的每个角色获得按公开顺序保留的必答槽位；
- 正常生成被拒或静默时，仅对当前必答角色启用经过同一安全边界的角色化有界回应；
- NPC 对 NPC 的同轮回答沿用同一必答保证，不再交由玩家转述；
- 自主步骤开始前重新检查已满足的终态，并在生成玩家发言前锁定发言权；
- 扩展拒绝转换的自然语言识别，覆盖“I'm pleased to confirm”等礼貌确认；
- 保留对 AI 玩家当前世界事实与外部产物断言的统一证据检查。

**保持不变：** Baseline、四个场景、固定模型、六维评价、匹配配对、冻结与哈希规则均不改变。G4.13 仍是探索性迭代，实时资格实验需另行获得推送与部署授权。

## 记录规范

每轮必须记录：

1. 调整前的问题与证据；
2. 新增或改变的架构机制；
3. 保持不变的实验边界；
4. 验证方法与观察结果；
5. 新暴露的问题；
6. 对论文研究问题、指标或有效性威胁的影响；
7. 对应代码提交。

## 第1轮：结构化多Agent场景

**目标：** 从单一大模型角色提示词升级为可配置多人情境。

**主要机制：**

- 角色身份、职责、倾向和关系；
- 私有状态、隐藏议程、红线和权限；
- 多角色独立发言；
- dispatch rules；
- phases、state schema和completion conditions。

**核心认识：** 语言风格不是角色真实性。真实角色还需要利益、知识边界、责任和不可越权的行动范围。

## 第2轮：Participation/Test双模式与统一导出

**目标：** 同时支持真人参与训练与AI对AI自动测试。

**主要机制：**

- participation mode：真人玩家与AI角色互动；
- test mode：AI玩家自动推进；
- `speaker_source=human|ai`；
- session、turn、sequence、speaker和message统一导出；
- 后台单步/连续运行。

**主要问题：** AI玩家最初仍以`user`表示，容易与真人混淆；自动模式比参与模式产生更多模型调用和长上下文。

## 第3轮：长对话与LLM韧性

**触发证据：** `finish_reason=length`、空content、502、`ERR_EMPTY_RESPONSE`。

**主要机制：**

- 空内容和length finish识别；
- 紧凑提示与有限历史窗口；
- 重试和fallback；
- 后端异常保护；
- LLM调用、预览、错误和重试日志；
- resilience smoke tests。

**核心认识：** 自动多人会话的稳定性不能依赖一次模型调用成功；长对话必须有有界上下文和可诊断失败。

## 第4轮：受控Baseline与批量实验

**目标：** 不把RoomMind与一个被故意弱化的聊天机器人比较。

**主要机制：**

- 中央提示词式多角色Baseline；
- RoomMind与Baseline共享公共场景、参与者、任务说明、玩家策略和模型锁定；
- 场景×条件×重复次数批量运行；
- 后台并发、恢复、取消和统一导出；
- matched pair与固定随机种子。

**核心认识：** Baseline必须获得同等公共信息，只缺少待检验的独立记忆、调度、状态治理等机制。

## 第5轮：生成、AI评价、人工盲评解耦

**目标：** 外部评价失败不能污染已经成功生成的对话。

**主要机制：**

1. 生成并冻结对话；
2. 独立AI六维真实性评价；
3. 可选人工盲评；
4. 最终证据报告；
5. forensic debug bundle。

**六个维度：**

- Role & strategic fidelity；
- Information boundaries；
- Temporal coherence；
- Interaction structure；
- Multi-party dynamics；
- Procedural fidelity。

**主要问题与修复：** evaluator曾将规范响应内部`metrics`误当包装层，造成全部评价失败；随后修复解析并保存每个维度的原始响应、错误和证据。

## 第6轮：开放场景治理与收敛

**触发证据：** 多个角色分别合理发言，但会议整体重复承诺、无限等待或运行到50轮。

**主要机制：**

- domain-neutral event ledger；
- work items；
- artifact offered/submitted/reviewed；
- action committed/completed；
- blocker、handoff、schedule、decision和outcome；
- evidence-grounded状态变更；
- progress signature和stagnation window；
- completed、conditional、deferred、failed、stalled；
- Agent contribution gate；
- 开放场景治理数据导出。

**对应提交：** `94d61fc Improve open scenario simulation convergence`

**验证结果：** 最新四个RoomMind场景分别运行44、29、21、21轮。事件账本和多终局确实运行，但工作项碎片化、状态缺失和伪进展仍让停滞计数频繁归零。

## 第7轮：事件语义规范化与强制收敛

**触发证据：** 批次`c532e2e7-078e-446c-bbaf-3097e20aa384`的forensic debug bundle。

发现：

- 供应链会话44轮、28个事件、16个工作项，其中12个仍开放；
- 产品发布29轮、20个工作项，其中16个仍开放；
- `information_provided`和`outcome`工作项可能没有status；
- 同一事项因`draft/details/summary`等措辞形成多个工作项；
- `artifact_reviewed`可以在没有`artifact_submitted`的情况下发生；
- 承诺和提案被计入进展；
- 第21至29轮之间只有7个连续无事件回合，恰好绕过8轮停滞阈值；
- 面试中多个NPC同轮提问，AI玩家回答了较旧问题；
- 明确宣布会议结束后仍继续生成。

**本轮机制：**

- task state升级为schema v4；
- 只有confirmed/rejected变量和submitted/completed/blocked/rejected工作项计入持久进展；
- promise/proposal不再重置停滞计数；
- 工作项支持稳定`work_item_key`、别名和确定性近似归并；
- 补齐information/outcome等事件的工作项状态；
- 只有required且未解决的工作项进入`open_issues`；
- 工作项保存milestones和last event；
- `artifact_reviewed`强制依赖`artifact_submitted`，非法转换保留在日志但不改变状态；
- 明确的meeting/interview/session closure直接形成completed/conditional/deferred终局；
- AI玩家提取本轮NPC直接问题，并优先回答最新具体问题；
- 增加对应回归测试。

**保持不变：**

- 不加入谈判专用流程；
- 不让后验评价影响对话生成；
- Baseline与RoomMind继续使用同一公共AI玩家策略；
- 角色私有记忆不进入共享治理状态。

**待验证假设：**

- 供应链和产品发布对话轮数应明显下降；
- repeated promise不应重置stagnation；
- 产品发布应在首次明确闭会时终止；
- 面试玩家应优先回应本轮最后一个具体问题；
- invalid artifact review应出现在forensic日志，但不进入完成状态。

## 第8轮：传统独立智能体Baseline

**触发原因：** 第4轮集中式Baseline虽然能够输出多个角色，但所有NPC由
一次中央模型调用生成、共享同一上下文，也同时暴露于所有角色私有资料。
这种比较适合衡量完整系统与集中式聊天的差异，却不能严格回答：在角色已经
是独立传统智能体并具有普通对话记忆后，RoomMind结构化认知与治理是否仍有
贡献。

**本轮Baseline定义：**

- 每个NPC独立调用相同平台模型；
- 每个NPC只获得自己的角色提示、private state和authority资料；
- 每个NPC分别持久化最近N条公开对话，属于普通滚动聊天记忆；
- 所有NPC获得相同公开场景和公开参与者名录；
- 每个NPC独立决定`speak`或`wait`；
- 不使用observation节点、reflection、planning或语义记忆检索；
- 不执行dispatch rules、authority enforcement、task state、event ledger、
  work items、evidence gate、completion correction或stagnation governance；
- 仅保留最大轮数、输出长度、格式修复和API重试等基础运行安全措施。

**新的核心比较：**

- C：传统独立智能体＋按角色隔离的普通滚动对话记忆；
- D：完整RoomMind结构化认知与治理架构。

**方法学意义：** 两组都已经是独立多智能体，且私有资料都按角色隔离。
主要处理差异不再是“一个模型还是多个模型”，而是RoomMind新增的结构化
认知、调度、权限、状态、证据和收敛治理机制。

**有效性边界：** C与D仍可能具有不同的模型调用次数和自然终止长度。
六维真实性的主要盲评应使用相同固定窗口；自然终止结果另行报告完成、延期、
失败、停滞和收敛轮数。

## 第9轮：运行时韧性与恢复体验

**触发原因：** 生产与预发日志显示，真实性架构本身之外，陈旧数据库连接、
WebSocket掉线、浏览器缓存失效会话、LLM欠费/下架/空响应，以及批量任务状态
耦合会直接中断用户任务。这些故障会把基础设施不稳定误记为模拟质量问题。

**本轮通用改进：**

- PostgreSQL连接池启用`pool_pre_ping`与定期回收，避免复用已关闭连接；
- WebSocket不仅在首次失败时重试，已连接后的异常断开也指数退避重连；
- 浏览器发现缓存session返回404时自动创建新session；
- `finish_reason=length`连续出现时压缩历史输入并保留系统指令、输出契约和最近对话；
- 参与模式在主提供商欠费、模型下架、持续5xx、空响应或传输故障时，可切换到
  另一个已经配置密钥的提供商；受控`test/baseline`实验明确禁用该切换；
- 单个失败对话可与外部评价并行重跑，不再因为批次处于评价状态返回409；
- systemd拆分守护API、Admin与Client，任一进程退出后独立自动重启。

**方法学边界：** LLM提供商降级只属于参与模式的可用性策略。论文批量实验继续
固定提供商和模型，因此不会因为容灾切换产生处理条件污染。技术故障、降级次数和
模型标签仍应保存在日志中，与真实性评分分开报告。

## 论文使用建议

## 第10轮：代际实验治理与真实对话专家盲评

**触发原因：** 此前虽然已经分离对话生成、AI评价与人工评价，但批次没有冻结
架构代际和研究用途；人工评分只绑定run id，没有绑定被评分对话的内容哈希；
管理页中的英文简表也不适合未来独立招募外部专家。

**本轮机制：**

- 注册`G0`历史开发代与`G1`治理型独立智能体代；
- 每个批次冻结generation、architecture version、source revision、研究阶段、
  random seed和manifest hash；
- 区分exploration、screening和confirmation，禁止把调参数据当确认性证据；
- 专家包只读取持久化`session_messages`，原文不翻译、不重写、不重新生成；
- 以规范化真实公共对话SHA-256绑定评分，提交时服务器重新校验；
- 新增独立中英双语专家页面，条件、模型、代际和内部状态全部隐藏；
- 六个维度各拆为三个1–7子指标，并保留序号证据、总体可信度和专家背景；
- 最终提交不可覆盖，为后续邮箱magic link或机构SSO预留身份边界。

**保持不变：** RoomMind与Baseline的对话生成机制、共享玩家政策和六维AI评价
本轮不调整。本轮首先解决研究证据治理和外部评价工具问题，不将界面改动宣称为
模拟真实性提升。

**下一步探索：** 在G1中先运行真实对话、自动指标和浏览器体验审计；若引入
事件溯源Turn Coordinator或确定性状态reducer等因果架构变化，则建立G2，不覆盖
G1结果。

**详细协议：** `docs/RESEARCH_EXPERIMENT_GOVERNANCE.md`。

## 第11轮：G1工程试运行与韧性资格门槛

**触发证据：** G1探索批次
`44a9e7b3-05e8-4903-ad01-42a67e7f0134`共24个匹配单元，最终只有8个
对话完成、16个发生技术失败。12个Baseline单元全部在第一自主回合失败；
RoomMind有4个单元在长会话中因模型没有可见输出而失败。该批次标记为
`engineering-invalid-pilot`，不进入真实性效果比较。

**确定根因：**

- Baseline首次读取尚未建立的角色滚动记忆时遍历`None`，因此12/12为确定性
  代码失败，并非Baseline模拟能力差；
- 一条RoomMind失败会话在第36回合前已累计319次成功LLM调用、467次请求尝试、
  133次重试以及约108万tokens；多阶段认知调用使低概率供应商空响应累积为
  较高的整场失败概率；
- 失败分别出现在公共AI玩家、角色私有决策和公开发言渲染环节；
- 运行任务缺少持久心跳，长时间`running`时无法区分正常计算与失去worker。

**G1.1修复机制：**

- 空的Baseline角色记忆规范化为空列表，并以真实首次回合作为回归用例；
- 将HTTP成功但无可见内容定义为独立异常类型，只对该可恢复故障降级；
- 公共AI玩家在两种条件中使用相同安全消息降级；
- Baseline角色空响应降级为`wait`，RoomMind私有决策空响应同样降级为`wait`；
- 初始计划使用确定性职责计划，反思失败时保留原观察并跳过该次反思；
- 公开发言渲染失败时使用既有角色fallback，而不是终止整场会话；
- 所有降级均写入`llm.degraded_fallback`遥测，并单独统计，不能静默混入结果；
- 发言配额已经用尽的非点名角色不再执行无必要的私有决策LLM调用；
- 同一角色反思设置最短三回合间隔；
- 检索记忆、公共时间线和任务治理视图设置字符与条目边界；
- 批跑在每个回合和尝试前持久化worker heartbeat、当前回合和最近完成回合。

**资格门槛：** G1.1不得直接进行大批正式实验。首先运行固定模型的小型
资格批次，每个条件至少覆盖一次真实首回合和一场完整会话；要求Baseline与
RoomMind均无代码异常、无永久running任务、对话可导出、哈希可复算，且所有
降级次数得到披露。只有资格批次通过后，才可创建新的探索批次。

**代际判断：** 本轮没有改变“传统独立记忆智能体C vs 结构化认知与治理D”的
核心因果架构，因此记录为G1内部候选版本G1.1，而不是提前启用G2。未来只有
引入事件溯源Turn Coordinator、确定性状态reducer或新的角色决策架构时才建立
新一代。

论文中应将这些轮次描述为“证据驱动的设计迭代”，而不是把每次修改都作为性能提升结果。每轮均应区分：

- 设计目标；
- 可观察失败；
- 架构机制；
- 验证数据；
- 新的有效性威胁。

最终系统评价必须使用第8轮之后重新生成的独立批次，不能把用于发现和修复问题的批次同时作为最终效果证据。第8轮之前标记为`baseline`的数据属于集中式Baseline，不得与第8轮后的传统独立智能体Baseline混合统计。

## 第12轮：G2确定性回合协调器

**触发证据：** G1.1主探索批次
`86f74b31-22ba-4af2-a4a9-7513eb5cce1b`完成24/24组对话和评价，但
RoomMind有11/12场由安全上限或停滞机制停止，程序真实性均值4.83，低于
Baseline的6.08。真实对话显示角色反复承诺稍后发送材料、等待模拟时间流逝、
重新打开已确认事项，并且发言调度没有优先选择当前阻塞事项的负责人。

**代际判断：** 本轮改变回合生命周期和状态转换语义，正式启用G2，而不是
继续把它记录为G1提示词小版本。

**新增机制：**

- 每回合从公共任务账本确定一个唯一焦点，不访问任何角色私有记忆；
- 工作承诺记录提出回合并随会话回合老化；
- 已到期承诺要求负责人执行、明确阻塞、移交或形成真实终局，不能再次承诺；
- 当前焦点负责人进入调度优先级，同时保留直接点名的最高优先级；
- 已确认字段锁定，普通新提案不能静默覆盖；只有有权限且有原文证据的明确
  重新确认、争议或拒绝才能改变状态；
- 停滞窗口或安全上限临近时进入结构化收尾，但协调器不能伪造完成事实；
- 焦点、负责人、到期状态和收尾指令进入forensic debug状态。

**保持不变：** Stanford式角色独立记忆、计划、检索、反思、私有信息边界、
同一公共AI玩家、固定模型和传统独立智能体Baseline均保持不变。

**G2资格假设：** 在不增加权限违规和秘密泄露的前提下，自然完成/真实延期率
应提高，安全上限与无进展停止率应下降，程序真实性不再系统性落后于Baseline。

## 第13轮：G2资格失败与G2.1证据落地协调

**触发证据：** G2资格批次
`2e9e1a8b-a03d-4b0e-90f2-0652bf7a77d5`完成8/8组对话及评价。RoomMind
互动结构为6.00，高于Baseline的5.25，说明唯一焦点和负责人优先调度发挥了
作用；但两个场景的程序真实性均为5.00，均低于Baseline的5.50。四场
RoomMind中三场任务化结束、一场达到安全上限。

**发现的问题：** 协调器追逐大量由普通对话产生的附带材料承诺，角色因此
声称发送附件、链接、哈希和现场结果；一次事故场景在三个必要条件仍未确认时
宣称完成。另有一次焦点owner写成`player`而非注册的`user`。批次manifest
未复制进session run_config，使G2专用探针错误显示为不适用。

**G2.1机制：** 将代际元数据冻结到每个会话；探针同时识别manifest和直接
architecture version；规范化玩家别名并跳过无法由NPC协调器路由的玩家独占
工作；到期承诺按承诺时间而非字段字母序选择；明确禁止虚构附件、链接、哈希、
测量、审批和越角色事实；对“公开宣称完成但必要条件未满足”的情况确定性降级为
conditional或deferred，并记录未满足字段。

**保持不变：** 独立角色记忆、计划、检索、反思、私有知识、共享公共玩家、
固定模型、传统独立智能体Baseline以及六维盲评协议均不改变。因此这是G2内部
修复候选G2.1，不与G2数据合并，也不提前宣称真实性提升。

**详细记录：** `docs/EXPERIMENT_G2_QUALIFICATION_RESULTS.md`与
`docs/EXPERIMENT_G2_1_QUALIFICATION.md`。

## 第14轮：G2.1资格结果——接线修复通过、真实性门槛失败

G2.1资格批次`3b9232e8-399e-4000-941d-b55abb3587a5`完成8/8组对话和
评价。哈希复算、独立记忆、协调历史、注册owner及完成条件一致性全部通过，
说明工程修复有效；但供应链两场均达到20回合安全上限，程序真实性仅3.5，
Baseline为6.0。事故场景虽然达到6.5，但仍出现虚构SHA-256和memory dump。

根因从“探针未接线”进一步收敛为“任务关键性判断错误”：任何附带材料承诺或
blocker都会成为required work，单个blocked项可连续垄断焦点，而真正的场景
完成条件仍停留在proposed。提示词接地不足以阻止公开发言本身虚构证据。

因此G2.1冻结为未通过资格的开发证据，不扩大实验。后续候选必须在确定性层面
区分task-critical与incidental工作、限制blocked焦点连续占用，并在公开发言输出
前执行证据接地校验。详细结果见
`docs/EXPERIMENT_G2_1_QUALIFICATION_RESULTS.md`。

## 第15轮：G2.2关键任务队列与公开证据闸门

**设计目标：** 直接修复G2.1已证实的两个因果问题，而不是继续增加角色提示：
附带承诺被错误升级为必要任务，以及虚构证据已经生成后才由状态评价器忽略。

**确定性任务关键性：** 状态评价器必须显式输出`task_critical`与公开理由；系统
只有在该事项与未解决状态字段重合，或原文存在required、must、cannot、until、
blocking等阻塞语义时，才接受它进入协调队列。普通附件、未来改进和礼貌性承诺
继续保留在事件账本，但标为incidental，不影响焦点。

**焦点轮换：** blocked或到期的关键工作先处理，配置状态其次，未到期工作最后。
同一个blocked事项最多连续占用两个普通工作焦点；存在其他状态时自动轮换，
不存在替代事项时转换为outcome-resolution，要求移交、拒绝、条件性结束、延期或
失败，不能再次陈述同一阻塞。

**输出前证据闸门：** NPC和AI玩家的公开文本在入库前检查。新造的附件/上传声明、
外部URL及32–64位十六进制哈希会被拒绝并重新生成；已在公共上下文出现的URL或
哈希可以引用。安全fallback同样经过检查。

**可观测性：** 新增grounding rejection、critical/incidental work、focus rotation、
outcome-resolution计数，并在G2.2探针中自动验证公开证据和工作焦点关键性。

**保持不变：** Stanford式独立记忆、计划、反思、私有边界、固定GPT-OSS 120B、
共享公共玩家、传统独立智能体Baseline与六维盲评协议均不变。详细资格协议见
`docs/EXPERIMENT_G2_2_QUALIFICATION.md`。

## 第16轮：G2.2资格失败与G2.3有界焦点、状态化证据校验

**触发证据：** G2.2资格批次
`a0a86a39-69ef-4da3-b76a-afb0ee0d5d25`完成8/8对话及48/48维度评价，哈希、
独立记忆与基本协调历史均完整；但四场RoomMind仅一场形成task-grounded条件性
终局，两场达到安全上限。供应链程序真实性5.0，低于Baseline的5.5。

**发现的实现缺口：** 自动grounding probe漏掉“已发邮件附件”“upload completed”、
`repo://`位置与checksum已验证等虚构完成事实。四场RoomMind共70个协调回合全部
聚焦state variable，没有一次work-item或outcome-resolution焦点；单一状态焦点连续
占用12–16轮，因此G2.2关键工作探针属于真空通过。评价补跑还会重新生成六个维度，
导致一个缺失维度补齐时另一个已完成维度可能被新失败覆盖。

**G2.3机制：** 将不可验证的外部完成动作、通用仓库URI和checksum验证声明纳入
输出前拒绝；任意普通焦点最多连续两轮，有替代事项则轮换，无替代事项则进入
`outcome_resolution`并要求真实确认、阻塞、移交、条件结束、延期或失败；新增最终
导出独立扫描、焦点连续性与outcome来源探针；评价重试只调用缺失维度，并以追加
方式保留已经完成的分数和遥测。

**保持不变：** 角色独立记忆、计划、反思、私有信息边界、GPT-OSS 120B、共享
公共玩家、传统独立智能体Baseline、场景信息和六维评价量表均不改变。详细协议见
`docs/EXPERIMENT_G2_3_QUALIFICATION.md`。

## 第17轮：G2.3资格失败与G3统一公共世界账本

**触发证据：** G2.3资格批次
`79078212-aab9-45fa-baf1-cc64ba880a13`完成8/8对话和48/48维度评价，技术失败为0，
转录哈希全部复算一致且互不重复。但四场RoomMind只有两场结束，另外两场因
`no_task_progress`停止；两场“完成”又存在过早完成。RoomMind六维均值全部低于
匹配Baseline：角色5.50对6.00、信息边界5.25对6.25、时序4.50对6.25、互动结构
5.75对6.00、多方互动5.25对5.75、程序真实性5.00对6.50。逐条精读458条公开
消息也显示Baseline在四个配对中均更自然连贯。
完整冻结结果见`docs/EXPERIMENT_G2_3_QUALIFICATION_RESULTS.md`。

**根因判断：** G2.3仍采用“先说话，后抽取状态”。`variables`、`work_items`、
`event_ledger`由状态评价模型从自然语言反推，协调器、发言安全器和完成判定读取的
不是同一个权威事实源。正则探针虽能拦截部分附件、链接和哈希措辞，却无法系统性
保证动作是否实际发生、谁有权限、何时发生以及能否计入任务完成。因此探针在存在
虚构状态页、附件、存储位置和已完成动作的对话上仍出现全通过。

**G3机制：** 新增唯一的`roommind-public-world-ledger-v1`作为公开世界真相源。
每个角色在自然语言生成前输出结构化public intent，系统按权限、simulation scope、
inline evidence和生命周期进行确定性校验。生命周期统一为proposed、committed、
in_progress、submitted、verified、accepted，以及rejected、blocked。外部动作不能在
文本会议中被当作完成；文档或验证只有把实际内容公开内联后才能进入终态。校验后的
意图再渲染成自然语言，若措辞越过已批准生命周期则拒绝重写；成功公开后才把事件、
角色、回合、tick、原话证据和provenance写入账本。

**统一读取：** 旧`work_items`保留为兼容读模型，但其物质状态由账本投影；后验LLM
抽取器只能附加原文证据，不能把没有账本支持的artifact/action提升为submitted、
reviewed或completed。协调器、进展签名、完成判定和导出都读取同一账本；存在必要
未完成工作时不得标记completed。新增账本存在性、事件来源、终态inline evidence、
统一时钟、生命周期和完成条件一致性探针。

**保持不变：** Stanford式每角色独立记忆、计划、检索和反思保持不变；公共账本
不是共享私有记忆。固定模型、共享AI玩家、传统独立智能体Baseline、匹配场景、六维
评价和独立人工盲评原则均保持不变。详细协议见
`docs/EXPERIMENT_G3_QUALIFICATION.md`。G3必须创建新批次，不能覆盖G1至G2.3数据。

## 第18轮：G3本地反例失败与G3.1规范实体、开放场景证据模式

**触发证据：** G3提交`dd070fa`尚未推送或部署时进行了本地预资格。四个确定性
反例全部失败：AI玩家无法接受其配置允许确认的字段；同一工作的action与
verification形成两个实体；自由改写subject可以重置生命周期；时钟倒退事件虽标记
无效仍会写入权威实体。权限检查还可因省略field而被material action绕过。

**历史规则回放：** 将G3证据规则回放到冻结的G1.1 GPT-OSS 120B数据，共检查
1,350条公开发言并标记34条。现场谈判、发布决策和事故指挥中的附件、邮件、链接
多为应拦截的虚构外部动作；但结构化面试中，候选人合理叙述过去发送邮件或附表的
经历也会被误判，证明开放场景不能共用单一live-operation语义。

**G3.1机制：** 玩家权限从task schema确定性派生；配置字段成为规范实体ID，非配置
物质工作在action、artifact、verification之间共享work实体并进行别名归并；每个
生命周期分别保留执行、提交、验证和接受者；倒退事件在改变实体前隔离；物质动作
必须匹配注册能力。新增retrospective evidence mode，面试中的历史经历作为公开事实
主张记录，而不推进当前世界动作；新造URL与哈希仍拒绝。

**实验纪律：** G3.1先完成本地反例、回放和匹配小样本测试。取得可复现阶段性成果
前不推送、不部署，也不创建新的服务器确认性批次。详细门槛见
`docs/EXPERIMENT_G3_1_LOCAL_QUALIFICATION.md`。

**本地压力实验补充：** 使用`qwen3.5:0.8b`只做失败注入，不作论文效果比较。
实验依次暴露了私密底价泄漏、内部fallback指令成为台词、LLM状态评估器从目标描述
复制未说出的字段值、请求被误记为已提交行动，以及同一事项因单复数形成重复实体。
相应加入protected-secret过滤、公开fallback语义、台词—结构化意图提交边界、字段/事件/
终局的确定性证据门槛和实体词形归一化。最终诊断运行在四回合后以
`no_task_progress`诚实停止，三项业务状态保持unknown且未制造协议；这证明的是弱模型
下的失效约束能力，而不是自然度优势。
同一角色对同一规范实体重复相同非物质转换也不再记为新事件；不同角色的首次提案
仍分别保留，以兼顾去重和多人来源审计。
配置字段的公开事件同时携带明确类型值；`accepted/verified`缺少公开值时不能推进，
不同值不能覆盖已接受结果。当账本中的接受者满足场景确认策略时，字段值确定性投影
到任务状态，从而消除“账本已接受、变量仍未知”的双事实源分歧。

## 第19轮：G3.1台词—账本分裂与G3.2公开语义蕴含闸门

**触发证据：** G3.1本地真实模型面试压力样本中，权威账本和任务字段正确保持
unknown，所有既有完整性探针也通过，但公开台词仍出现“工程负责人已经确认、可以
继续推进”。进一步实验发现，强制fallback没有携带已验证public intent；面试场景的
retrospective策略又被错误继承到当前时态fallback；确定性的未来被动语态“will be
accepted”也可绕过只检查已完成时态的旧规则。

**G3.2机制：** 在结构化意图验证与公开消息持久化之间增加生命周期语义蕴含闸门。
第一、第三人称、主动/被动以及确定性未来终态表达均映射到最低隐含生命周期，台词
不得超过已批准意图；if、whether、need to、could等询问和条件语言保留。retrospective
不再是场景级免检开关，每一句都必须包含明确历史锚点，否则按现场陈述拒绝。强制
fallback固定为当前discussion意图，读取同一task ledger，并经过相同台词安全边界；
两次重写仍失败时只输出中性澄清句。

**本地结果：** 三类真实反例均转化为确定性回归测试；直接renderer失败注入全部降级
为安全澄清句；四类开放场景8个脚本化工程对话均完成，RoomMind导出全部通过适用
完整性探针且没有制造完成。弱模型仅用于失败注入，不构成自然度比较。详细记录见
`docs/EXPERIMENT_G3_2_LOCAL_PREQUALIFICATION.md`。

## 第20轮：G3.2资格失败与G3.3语境化发言边界

**冻结结果：** G3.2预发批次
`7135ae99-d1c6-4f3a-842b-93a8a832009c`完成8/8对话和48/48维度评价，技术失败
和降级均为0，所有适用G3账本探针通过。但四场RoomMind全部以
`no_task_progress`停止，outcome-resolution为0。RoomMind六维中五维低于Baseline，
互动结构2.75对4.25，程序真实性3.50对4.75。详细冻结证据见
`docs/EXPERIMENT_G3_2_QUALIFICATION_RESULTS.md`。

**根因：** G3.2把回顾性真实性绑定到少数固定历史短语，正常的`I led`、
`I managed`、`One project`等面试回答被误拒绝；不构成事实主张的问题句也因错误的
structured scope被拒绝。两次失败后，玩家和NPC都回退为同一个“澄清最高优先级问题”
模板，形成自我强化循环。由于玩家策略为两组共享，Baseline也受到同一误伤，G3.2
不能作为有效的处理效应比较。

**G3.3机制：** 保留权威账本及全部当前事实/外部动作限制，扩大自然过去时锚点；
只有实际越过生命周期的未锚定回顾性措辞才拒绝，非断言问题不误伤。共享玩家按
retrospective/live语境生成不同且轮换的安全继续动作；RoomMind NPC fallback读取已
验证subject和公开职业身份，并再次通过相同安全边界，不再使用单一全局澄清句。

**本地证据：** 全套smoke和应用导入通过；目标模型系列`gpt-oss:120b-cloud`的
4个面试+3个事故探针中，面试均直接回答具体历史案例，事故均拒绝虚构已完成动作，
fallback计数为0。当前仅为本地工程预资格，尚未推送或部署。详细门槛见
`docs/EXPERIMENT_G3_3_LOCAL_PREQUALIFICATION.md`。

## 第21轮：G3.3稳定但失真与G3.4自然恢复、有限证据闭环

**冻结结果：** G3.3预发批次
`560252ab-1626-452b-801b-0ad0396e50b8`完成8/8对话和8/8六维评价，技术失败与
降级回复均为0，八份对话哈希复算全部匹配。但RoomMind仅在角色/策略一致性和时间
连贯性各领先0.25，在信息边界落后0.75、互动结构和多方动态各落后1.00、程序真实
性落后0.25。四个RoomMind运行全部stalled。

**人工精读根因：** RoomMind出现31条可见治理模板话术，Baseline为8条；系统把
无法在文本会话内交付的附件作为持续阻塞条件，形成“请求上传—承诺上传—再次请求”
循环。结构化面试又在等待附件后虚构上传、仪表盘编号、指标及负责人。原有
`llm_degraded_fallback_count=0`未记录安全renderer fallback，造成可观测性盲点。

**G3.4机制：** 保留G3权威账本和权限边界，将fallback改写为基于已验证议题的自然
任务语言；外部文件只允许成为会后交付，现场必须给出可陈述证据并进入条件决策、
延期或handoff；增加“疑问不构成事实证据”的确定性闸门；新增
`dialogue_safe_fallback_count`。共享玩家继续在两组保持相同政策，避免把处理效应混入
对照条件。本地真实模型压力测试还发现“需要确认校验和是否已验证”被误判为已验证，
现已把confirmation-request语法纳入非断言边界。详细本地门槛见
`docs/EXPERIMENT_G3_4_LOCAL_PREQUALIFICATION.md`。

## 第22轮：G3.4工程稳定但真实性资格失败

**冻结结果：** G3.4预发批次
`b1d3699d-d697-449d-8cca-ebe776712a74`在固定`ollama/gpt-oss:120b`下完成
8/8对话和8/8独立六维评价，技术失败与降级回复均为0，八份转录哈希复算一致。
新增安全fallback可观测性成功暴露了此前隐藏的恢复路径：RoomMind共记录58次安全
fallback和159次公开输出拒绝，Baseline分别为15和48。

**真实性结果：** RoomMind六维均值全部未超过Baseline：角色/策略4.50对5.00、
信息边界5.00对5.50、时间连贯4.00对5.75、互动结构3.25对4.75、多方动态4.25
对5.50、程序真实性4.25对5.50。只有结构化面试场景在角色、互动和程序三个维度
局部领先；谈判、发布决策和事故指挥没有复制这一结果。样本只有四个匹配对，属于
探索性开发证据，不能作总体统计推断。

**人工精读根因：** 公开发言与权威状态投影仍然分裂。角色已经在台词中明确确认
价格、交付或决策，账本却只记录信息提供，任务字段继续停留在proposed；协调器于是
反复追问，四个RoomMind运行全部达到20回合并stalled。输出安全器在完整台词生成后
频繁拒绝和重写，最终形成可辨认的fallback语言。更重要的是，“角色说过某事”只能
证明该陈述存在，不能证明上传、哈希验证、健康检查、回滚或审批等外部动作真实完成；
现有确定性探针因此能全部通过，却仍漏掉人工可见的虚构当前世界事实。Baseline也有
循环和事实矛盾，例如事故场景同时声称回滚到v4.17以及100%流量恢复到v4.18，但其
对话受到的治理摩擦更少。

**资格结论：** G3.4不通过真实性资格，仅作为工程稳定性和根因诊断的冻结开发证据，
不进入人工盲评，也不能用于论文中的RoomMind优势确认。完整证据见
`docs/EXPERIMENT_G3_4_QUALIFICATION_RESULTS.md`。

**评价审计限制：** 48个维度虽然都被系统标为completed，但Baseline场景3的时间
连贯性和RoomMind场景4的程序真实性存在“维度总分有效、全部子指标却为空理由默认
1分”的内部不一致。当前完成判定只检查dimension score是否存在，因此这两项属于
结构性假完成。冻结结果不事后覆盖，数值表原样保留并降级为临时证据；后续评价器
必须同时验证全部子指标、理由和公开证据引用，且与对话生成链路继续保持独立。

**下一代方向：** 若继续，必须定义为G3.5：将授权确认的台词与任务状态原子提交；
把证据来源区分为场景种子、公开陈述、模拟工具结果和会后外部事项；当前世界动作的
完成只能由模拟工具结果支持；在生成前约束意图并只局部修复违规子句；当现场证据
耗尽时强制条件结束、移交、延期或失败；同时以通用账本承载不同开放场景各自的合理
完成语义；评价器增加子指标完整性闸门。G3.4冻结数据不得覆盖或重算。

## 第23轮：G3.5原子确认、来源化证据与评价完整性

**问题：** G3.4人工精读发现三类系统性断裂。第一，公开台词中的单方明确确认会因
聚合状态仍为proposed而丢失，协调器持续重开已解决议题。第二，“角色说过”仍可能
被当作“现实动作已发生”，公开发言不足以证明回滚、上传、健康检查或哈希验证完成。
第三，六维评价只凭维度总分即可完成，无法识别子指标默认值、空理由和空证据造成的
结构性假完成。NPC在有效公开草稿后仍进行整段二次改写，也制造了大量安全拒绝和
fallback。

**G3.5机制：** 将每位有权限参与者的明确、值一致、原文可定位确认原子提交到公共
账本，再由原有确认策略决定何时聚合完成。证据来源显式分为scenario_seed、
public_statement、simulated_tool_result和external_followup；当前世界动作只有在引用
引擎预先登记且参与者、字段、内容完全匹配的模拟工具结果时才能进入终态，伪造结果
编号自动降级。通过全部安全边界的public draft直接发布，避免无必要的全句再生成；
证据耗尽的outcome-resolution回退改为明确延期和移交。独立评价器要求每个维度的
全部子指标都有合法分数、理由和真实转录证据编号，否则重试并保持缺失。

**本地结果：** compileall、公共账本、台词安全/状态、LLM韧性、研究协议及diff检查
全部通过；新增反例覆盖跨回合双边确认、伪造工具编号、导出探针与工具注册表不一致、
有效草稿直发、证据不足延期和评价结构假完成。本机Ollama端点未运行，因此当前仅为
确定性工程预资格，不包含真实模型自然度结论，也不证明RoomMind优于Baseline。
详细记录见`docs/EXPERIMENT_G3_5_LOCAL_PREQUALIFICATION.md`。

## 第24轮：G3.5评价可信度提升但对话真实性资格失败

**冻结结果：** G3.5预发批次
`5bc77e6d-822e-42d1-9ad2-dbd97a0a33ba`在固定`ollama/gpt-oss:120b`下完成
8/8对话。首次评价因新完整性闸门拒绝两个结构不完整维度；仅重试缺失项后达到
48/48，已完成维度未覆盖，全部子指标、理由及转录证据编号通过复核。八份转录哈希
复算一致，无对话失败或LLM降级。这证明G3.5解决了G3.4的“评价假完成”，但不证明
模拟真实性。

**真实性结果：** RoomMind六维均低于Baseline：角色/策略3.50对6.50、信息边界
4.00对6.25、时间连贯4.00对5.00、互动结构3.50对5.75、多方动态4.75对5.25、
程序真实性4.50对6.00。四个RoomMind运行全部stalled；场景1和4达到20回合安全
上限，场景2和3因无任务进展提前结束。结果仅是四个匹配对的探索性开发证据。

**人工精读根因：** 来源化账本能阻止无工具证据的现实动作进入权威终态，却不能阻止
同样的虚假动作出现在公开台词中，因为LLM意图误分类可绕过动作校验。原子确认没有
贯穿类型归一化、权限解析和聚合投影；谈判中`84 RMB`被保留为字符串，角色已公开确认
而字段仍为proposed。有效草稿直发在八个运行中的实际使用次数为0，主要生成路径仍是
拒绝、整句重写和可辨认的fallback。系统还要求文本世界提供附件、哈希、健康检查等
本身不能生成的证据，导致虚构完成或无限等待。

**资格结论：** G3.5不通过真实性资格，不进入外部人工盲评。它作为“严格评价器有效、
对话治理仍失败”的冻结开发证据保留。下一次架构变更必须定义为G3.6：在公开文本写入
前进行确定性的分句动作/确认解析和类型归一化；无注册工具结果的完成态台词必须改写为
承诺；fallback只局部修复且不公开通用模板；协调器必须读取场景能力，不能追逐文本环境
无法产生的现场证据。完整证据见`docs/EXPERIMENT_G3_5_QUALIFICATION_RESULTS.md`。

## 第25轮：G3.6台词级事实边界与环境能力感知

**目标：** 修复G3.5“账本不接受虚假动作，但观众仍能听到虚假动作”的断裂，同时解决
带单位数值无法归一、评价器漏填confirmed_by导致公开确认丢失、协调器追逐文本环境无法
产生的结果，以及全局fallback模板破坏角色自然度。

**G3.6机制：** 在台词写入前独立于LLM意图标签逐句识别当前世界完成态；只有已登记的
simulated_tool_result能够支持执行、部署、回滚、上传、哈希验证、健康检查和发布等终态。
混合台词只删除违规子句并保留安全完整子句。公开、明确、非条件且有权限的字段确认由
确定性投影器直接提取，并先按schema归一类型和单位；可执行字段仍必须有工具结果。
协调器对缺少工具能力的执行字段使用capability_boundary，要求条件结束或会后移交。
两次修复仍失败且场景未配置安全公开回复时，角色保持沉默，由其他参与者接续，不再把
通用治理模板当作台词。

**本地证据：** compileall、公共账本、台词安全/状态、LLM韧性、研究协议和diff检查均
通过。只读重放G3.5冻结对话，新探针识别出RoomMind谈判中的2条虚假附件声明及事故指挥
中的9条无工具完成态声明；这些正是G3.5人工精读指出而旧探针遗漏的内容。当前结果只构成
确定性工程预资格，尚未证明新生成对话更自然。详细记录见
`docs/EXPERIMENT_G3_6_LOCAL_PREQUALIFICATION.md`。

## 第26轮：G3.6局部真实性改善但整体资格失败

**冻结结果：** G3.6预发批次
`8d1ff739-be70-4e45-8feb-3826f37061bb`在固定`ollama/gpt-oss:120b`下完成
8/8对话，技术失败和LLM降级均为0，八份转录哈希复算一致。独立评价首轮完成
47/48维度，仅补评缺失项后达到48/48；288个子指标均有合法分数、理由和原文证据。

**评价结果：** RoomMind相对Baseline在角色/策略持平（5.50对5.50）、信息边界
略高（5.00对4.75）、时间连贯略低（4.25对4.75）、互动结构略低（5.00对5.25）、
多方动态持平（5.00对5.00）、程序真实性略高（5.00对4.50）。G3.6不再出现G3.5
六维全部落后，但四个匹配对只能作为描述性开发证据，且四场RoomMind仍全部stalled。

**人工精读结论：** 事故指挥是最清晰的局部改进：Baseline大量虚构日志、快照、
回滚、哈希和发布动作，RoomMind能够承认文本会话无法取得验证结果并延期。但RoomMind
仍漏过`All traffic ... has been blocked`这类被动完成态，也漏过`Sample log entries
attached`这类简写附件声明；进入capability boundary后，后续回合仍反复索要同一日志。
谈判和发布决策继续出现确认后重开、附带议题升级和未能收尾；面试相对Baseline更好，
但仍重复提问并产生相互冲突的技术与结果数字。有效草稿直发、分句修复、静默恢复和
原话确认提交在四场RoomMind中均为0，说明若干G3.6核心路径没有在真实批次中生效。

**资格结论：** G3.6工程稳定且在外部动作克制上获得局部真实性进步，但没有解决
整体收敛和台词—状态一致性，因此不通过资格、不扩大实验、不进入外部人工盲评。
下一候选定义为G3.7：使用规范化命题覆盖主动/被动/完成时及语义改写；将能力边界持久化
为不可重复请求的解决状态；从权威原话命题确定性投影确认；增加completed/conditional/
deferred/failed闭合reducer；附带事项不得在主决策后升级为必要阻塞；公共AI玩家移除
治理模板并清洗破损引用。完整证据见`docs/EXPERIMENT_G3_6_QUALIFICATION_RESULTS.md`。

## 第27轮：G3.7规范化命题、持久能力边界与确定性收尾

**目标：** 直接修复G3.6冻结对话中已经观察到的反例，而不是扩大场景或增加抽象评分。
核心问题包括被动完成态和省略式附件声明漏检、一个字段的条件语句误伤同轮另一个字段的
无条件确认、缺少工具能力时重复索要同一证据、附带议题升级为新阻塞，以及长消息按字符
截断后把半截单词复制进公共台词。

**G3.7机制：** 新增公开子句规范化层，将可见语言归约为对象、终态谓词、模态和类型，
统一处理主动、被动、完成时和简写完成态，且不信任模型提供的intent标签。确认投影改为
字段—子句原子处理，使价格的无条件确认不会被另一句有条件的交付说明抵消。协调器首次
发现无注册工具结果的执行字段后，将其写入持久capability boundary，后续不再重复选择；
当所有剩余条件都属于这种边界且其他必要工作已解决时，确定性reducer以conditional收尾，
但绝不把现实动作标成已完成。schema驱动任务中的新附带事项只有与未决字段重合才能升级
为必要工作。共享玩家只提取完整问句并清除结构化残片，恢复话术改为普通会议语言。

**本地证据：** 六个既有smoke/导入/编译检查全部通过；新增回归覆盖G3.6原句
`All traffic ... has been blocked`、`Sample log entries attached`、已送达邮件、混合条件确认、
一次性能力边界、条件收尾、附带事项隔离和长消息完整问句提取。导出与批次摘要增加持久
边界和边界收尾字段，G3.7探针验证边界不重复以及conditional结果与未满足字段一致。
本地0.8B模型探针未在可用时间内完成。获得单独外发授权后，固定
`ollama/gpt-oss:120b-cloud`组件探针又发现并修复三项真实模型问题：768 token结构化输出
被推理占满而截断、含糊追问时静默切换面试案例、以及后半句保留意见掩盖前半句虚构证据。
生成预算现限定为1024--1536 token但公开台词仍限制120词；两组共享玩家从公开历史中获得
相同的最近案例锚点；证据归因按子句及相关对象核验，负面的哈希信息不能为正面的回滚结论
背书。最终固定模型复测无fallback，面试案例连续，事故回滚与校验状态均保持未确认。
这只使G3.7通过固定模型本地工程预资格，尚未构成完整RoomMind/Baseline真实性比较，也尚未
推送或部署。详细记录见
`docs/EXPERIMENT_G3_7_LOCAL_PREQUALIFICATION.md`。

## 第28轮：G3.7完整批次揭示三套状态未能收敛

**冻结结果：** G3.7预发批次
`b60e22d3-20b7-4818-b85b-027d3a1a4d18`在固定`ollama/gpt-oss:120b`下完成
8/8对话且无对话失败。八份转录哈希复算一致；独立评价首轮46/48，仅补评两个缺失
维度后达到48/48，288个子指标结构完整。七份转录通过全部适用确定性探针；RoomMind
面试在序号23因无模拟工具结果的当前世界完成态声明未通过。

**评价结果：** RoomMind相对Baseline在角色/策略（5.25对5.00）、信息边界
（5.75对5.25）和多方动态（5.75对4.75）占优，但在时间连贯（4.00对5.25）、
互动结构（5.00对5.50）和程序真实性（4.25对5.50）落后。四个RoomMind运行全部
stalled：两个达到安全上限，两个因无任务进展结束；原话确认提交、有效草稿直发、
分句修复和静默恢复仍全部为0。

**人工精读：** RoomMind的角色分工、职能冲突和信息分离通常比Baseline丰富，尤其面试
前半段明显更像多人评审。但谈判与发布会在主要决策已公开确认后升级附件、预算、保修等
附带事项；面试在证据已确认且候选人提问结束后转为KPI/Confluence项目会；事故指挥虚构
精确S3桶、哈希、WAF规则、deployment ID和健康指标，并在回滚完成后重新要求启动隔离。
Baseline也大量虚构现场动作且严重重复，但多数配对中更能保持既有决定并收尾。

**根因：** 公开台词命题、work item状态和completion variable仍是三套不能原子同步的
事实来源。面试中的必要work item已经completed，但对应字段仍unknown/proposed；事故中
负责人已公开批准、指派和确认，字段仍未confirmed。协调器于是继续轮转陈旧焦点，附带
事项继续进入工作图，最后以停滞或安全上限退出。G3.7的规范化、持久边界和closure reducer
并未获得它们所需的统一权威输入。

**资格结论：** G3.7不通过真实性资格，不进入外部人工盲评。下一候选不再增加提示或
评价维度，而应把有权限的公开原话命题、必要工作状态和完成字段合并为一个原子reducer；
主条件闭合后启用closure lock，禁止可选跟进重新打开已解决字段；所有现场ID、指标、附件
和发布动作必须绑定source-typed simulated result。完整证据见
`docs/EXPERIMENT_G3_7_QUALIFICATION_RESULTS.md`。

## 第29轮：G3.8统一权威状态归约与收尾锁

**目标：** 修复G3.7完整批次揭示的根本问题：公开台词、work item和completion variable
分别演化，导致参与者已经确认而系统仍继续追问；同时防止主条件闭合后，附件、仪表板、
会议邀请等可选跟进再次成为必要阻塞。

**G3.8机制：** 确认语法扩展到真实商务表达，包括`I can confirm`、`I hereby approve`、
`covers/satisfies the evidence`、`consider ... complete`以及明确设置时间和指派负责人的说法；
仍拒绝问题、条件句、否定句、无权限确认及无模拟工具结果的可执行字段。配置字段先于派生
work item成为权威状态；与已确认字段或持久能力边界对应的必要工作保留审计记录，但退出
收尾图。全部配置条件满足时写入closure lock，后续通用事件不能再把可选事项升级为必要
工作；只有有权限的字段挑战才能重新打开。

**本地证据：** G3.7面试原句回放现在能够提交产品、工程、领导力和候选人提问确认，满足
多人确认策略后任务完成并写入closure lock；同轮预置的可选KPI仪表板不再阻塞收尾。
事故指挥回放中，恢复方案、客户沟通负责人和20分钟复盘点均能完成多人确认，但没有注册
工具结果的containment仍保持unknown；系统只对该能力边界作conditional收尾，不再循环或
虚构执行成功。多字段work映射要求至少两个语义词重合，避免仅凭`review`等泛词误关闭事项。
台词安全、公共账本、研究协议、LLM韧性和compileall检查均通过。数据库模式smoke因本机
`127.0.0.1:5432`未运行而无法执行，尚不构成端到端或真实性资格。详细记录见
`docs/EXPERIMENT_G3_8_LOCAL_PREQUALIFICATION.md`。

## 第30轮：G3.8完整批次揭示多人确认策略的最后断点

**冻结结果：** G3.8预发批次
`5086c86f-6a49-4e4c-9ca0-ff6dc4222470`在固定
`ollama/gpt-oss:120b`下完成8/8对话且无对话失败，独立评价经仅补缺失维度后达到
48/48。六维总均值RoomMind与Baseline均为5.625；RoomMind只在互动结构和程序真实性
略高，在角色策略和时间连贯性略低。四个RoomMind运行仍全部stalled，原话确认提交和
closure lock均为0。

**有效机制与新断点：** G3.8公共账本已把多个价格、交付、质量、发布和面试命题记录为
accepted，并在发布和事故场景各归约两个派生work item，说明权威账本和work归约已经真实
进入端到端路径。但要求“玩家+对手/负责人”的字段只记录了NPC一侧确认：谈判三个字段均
停在proposed，发布只剩launch_decision，面试只剩candidate_questions_complete。事故中
“I confirm I'm the owner”和有权限的排期表达不能生成字符串负责人及复盘时间；无工具结果
的containment则被正确拒绝。

**人工精读：** RoomMind面试明显比Baseline少角色串线、问题更分工，但仍在候选人与
产品负责人都结束提问后多跑六轮，并虚构ADR、经理原话、Grafana状态和招聘时间。谈判在
核心条款公开确认后新增不属于质量负责人的财务审批；发布在联合批准后重新打开人员缺口；
事故在初期更审慎，但仍允许健康检查、快照ID、write-block和附件日志等无工具现场事实进入
可见台词。AI评分把许多“前文自洽的虚构”误判为有依据，不能单独作为真实性结论。

**资格结论：** G3.8不通过，不进入外部人工盲评。根因已从“三套状态完全分叉”收窄为
“多人确认政策的玩家一侧没有原子落账，以及真实自然的指派/排期/收尾表达没有被规范化”。
下一候选G3.9应在公平的公共信息玩家策略中明确完成自己应承担的确认，并加入有边界的
总结批准、无更多提问、第一人称负责人绑定和授权排期归约；同时让stalled、缺少必要锁、
accepted账本与变量不一致直接使研究完整性门失败。完整证据见
`docs/EXPERIMENT_G3_8_QUALIFICATION_RESULTS.md`。

## 第31轮：G3.9自然多人确认与非空洞收尾门

**目标：** 修复G3.8剩余的多人确认断点，同时保持RoomMind/Baseline的玩家信息基础一致，
并防止完整性探针在所有运行均stalled时仍报告通过。

**G3.9机制：** 公共信息玩家在对手已明确接受具体值且自己确实同意时，会公开说出自己一侧
的确认，不读取RoomMind私有状态，也不要求已确认角色重复。确定性归约器新增有边界的联合
批准、mark/record complete、无更多问题、授权排期和负责人自我认领；枚举值支持下划线到
自然空格、单位支持安全单复数、长句证据不再只看前12个词。负责人第一人称认领只能绑定到
该发言者已注册的显示名。问题、请求、否定、条件承诺、越权确认和无工具执行声明仍被拒绝。

**本地证据：** 冻结G3.8的四类失败均已变成回归：谈判三项联合确认、发布联合批准、面试
双方自然结束提问、事故30分钟复盘与客户沟通负责人指派均能完成并写入closure lock；
`Please confirm`负例保持unknown。台词安全、公共账本、研究协议、LLM韧性、compileall和
diff检查通过。G3.9新增三项非空洞门：RoomMind不得stalled；completed必须有closure lock；
账本参与者已满足确认策略时，变量值、状态和确认者必须原子一致。测试同时证明状态分叉会
使探针失败。

**当前边界：** 本机检出没有`.env`或本地PostgreSQL实验栈，因此尚未开展数据库支持的固定
模型配对实验，不能声称真实性提升，也不能进入外部人工盲评。完整本地预资格证据见
`docs/EXPERIMENT_G3_9_LOCAL_PREQUALIFICATION.md`。

## 第32轮：G4.0有界收敛通过稳定性、未通过真实性资格

冻结批次完成8/8对话、48/48独立维度评价且哈希全部复算一致。RoomMind四个场景均不再
以`stalled`结束，证明有界conditional/deferred收尾机制生效。但事故场景仍有三处同角色
近重复，面试场景发生工程负责人替候选人回答个人经历的角色串线；独立AI评分也未显示
RoomMind总体领先。因此G4.0不晋级。完整证据见
`docs/EXPERIMENT_G4_0_QUALIFICATION_RESULTS.md`。

## 第33轮：G4.1发言权与言语行为所有权

**目标：** 修复G4.0的两个可复现公共交互缺口，而不添加场景特例。

**机制：** 模型输出和确定性回退统一执行同角色重复保护；NPC直接向玩家提问后立即移交
发言权；内部决策意图为询问玩家而公开台词却替玩家作答时拒绝发布；全景trace保存新增的
handoff和speech-act rejection事件，并在冻结导出中复算同回合抢答。

**研究边界：** 这些确定性测试只能证明机制按设计运行。是否改善自然度和多人真实性，仍需
固定条件下重新生成配对对话、独立评价并逐条人工精读。

## 第34轮：G4.2有依据的受话人与确认对齐

**G4.1冻结结果：** 8/8对话、48/48独立评价和全部哈希校验完成，但发布会与面试两个
RoomMind运行仍然stalled，因此不晋级。人工复核发现三个所谓“玩家发言权违规”中有两个
是检测器把NPC之间的实名提问误判为向玩家提问；另一个是真实的双重追问。更关键的是，
发布会CFO和面试产品负责人都已经生成了自然、明确且有权限的最终确认，却因结构化intent
仍标成普通statement而被安全层静默拦截。完整证据见
`docs/EXPERIMENT_G4_1_QUALIFICATION_RESULTS.md`。

**G4.2机制：** 所有公开NPC发言都持久化grounded public intent，不再只在生成台账事件时
保存；新增统一受话人解析器，以公开原文中的实名/常用名地址优先、注册target次之，随后才
使用保守的第二人称玩家推断，并在落库前完成对齐。明确、无条件、第一人称且有字段确认权限
的台词，可以把模型误标的generic intent确定性对齐为accepted，但只能采用状态中已经存在的
值，不能创造新协议。问题、条件句、未知值和越权表达仍不会升级。

**研究边界：** 这一轮仍不改变各角色的独立记忆、感知、计划、反思与行动循环；它修复的是
多智能体公共接口的语义一致性。只有新的冻结配对实验、完整外部AI评价和逐条人工精读才能
判断真实性是否改善。预资格方案见`docs/EXPERIMENT_G4_2_LOCAL_PREQUALIFICATION.md`。

**G4.2.1工程补丁：** 首个G4.2冻结批次完成6/8；两个RoomMind运行在首次触发玩家确认
对齐遥测时因API模块缺少`emit`导入而失败，且降级回退均为0。该批次保留为无效工程证据，
不在原批次混入新代码重试。G4.2.1只补齐导入和模块边界回归，不改变模拟机制，并以新的
源码修订和全新8条配对批次重新验证。

## 第35轮：G4.2.1完整批次验证接口有效但仍未通过真实性资格

**冻结结果：** 新批次`fbeacf41-d13b-449a-ad7d-9783442d6823`在固定
`ollama/gpt-oss:120b`下完成8/8对话、48/48独立维度评价，零对话失败、零LLM降级回退，
8个转录哈希全部复算一致。受话人对齐触发6次、授权确认对齐触发7次；所有RoomMind运行
均通过结构化问题目标与公开台词一致性探针，说明G4.2窄接口机制确实进入端到端路径。

**仍然失败：** 谈判和事故指挥两个RoomMind运行达到20轮安全上限并以`stalled`结束。
产品发布和面试正常写入完成状态，但发布会在未披露预算金额和应急额度时过早结束；事故
指挥在SRE已公开表示containment仍active并批准回滚后仍未闭合。说明受话人和intent对齐
解决了“谁在问谁、明确确认被误标”的局部问题，但公共语义、实质证据和任务完成状态之间
仍存在断点。

**评价与精读：** 六维AI描述性总均值RoomMind为5.667、Baseline为5.542，差值仅+0.125；
RoomMind只在面试配对明显胜出，其他三对AI均分更低。逐条人工精读形成混合2比2判断：
RoomMind在谈判中较少虚构外部执行、在面试中显著减少多人重复追问；Baseline在发布决策
实质性和事故指挥流程上更完整。两边仍存在保护信息泄露、无依据精确数据、代替他人回答、
虚构文档或现场动作等问题。

**资格结论：** G4.2.1不晋级，不启动外部人工盲评。它是一次有效且技术稳定的失败资格
实验：证明接口修复有效，但没有证明总体真实性提升。后续代际必须同时约束玩家代理不得
替角色作证、把对话内文档/数据纳入能力边界、区分形式确认与实质证据完整性，并修复自然
确认到任务状态的最后投影缺口。完整证据见
`docs/EXPERIMENT_G4_2_1_QUALIFICATION_RESULTS.md`。

## 第36轮：G4.3跨角色发言权与现场证据边界

**目标：** 直接处理G4.2.1逐条精读暴露的两个跨场景缺陷：玩家替被点名NPC回答或作证，
以及角色把未经系统工具验证的现场报告、证书、日志、测量或动作状态当成已经存在的事实。

**机制：** RoomMind与Baseline玩家都只使用相同的公共参与者目录解析受话人；NPC对NPC的
直接问题由编排器保留为待响应义务并优先交给被点名角色。若玩家先取得发言权，只能做简短
交接，不能替目标角色确认、承诺或提供证据；目标角色已经回应后，待答问题会被清除。安全层
新增通用现场证据制品边界，并把“仍处于激活状态”“规则已经应用”等表达归一为需要模拟
工具结果支撑的当前世界终态声明。冻结导出新增跨角色问题所有权确定性探针。

**本地证据：** 新增正反例覆盖纯交接、先替答后交接、未交接、被点名角色已经回答、未经
工具支撑的报告/证书/现场状态以及允许的普通草案。公共账本、研究协议、言语安全与任务状态、
LLM韧性四组smoke测试通过，compileall与diff检查通过。数据库依赖的模式测试留给预发环境。

**研究边界：** G4.3仍是候选机制，不是更高真实性的结果。它不改变独立记忆、感知、规划、
反思和行动循环，也不把私有状态提供给玩家或Baseline。必须新建固定模型的8条配对冻结实验，
随后独立AI评价、哈希与完整性复算，并逐条精读全部四对原始对话；外部人工盲评不会自动启动。
完整预资格方案见`docs/EXPERIMENT_G4_3_LOCAL_PREQUALIFICATION.md`。

## 第37轮：G4.3端到端资格失败

**冻结结果：** 批次`ea08dd0f-bbec-45a7-93d1-dd8579281211`完成8/8对话、48/48
独立AI维度评价，零技术失败，8个转录哈希复算一致。RoomMind六维描述均值仅比Baseline
高0.042；信息边界和多方动态较高，但角色策略和程序真实性明显较低。

**失败证据：** 谈判仍有两次玩家替被点名角色先行作证；产品发布达到20轮并以stalled
结束；面试虽短但未完整结束；事故指挥虚构哈希已经发布、核验和不可变，又把Emily Chen、
Alex Patel等未注册姓名变成会内阻塞责任人并持续等待。G4.3的提示式约束和部分安全模式
能够减少某些问题，但没有形成可靠的公共交互不变量。

**资格结论：** G4.3不晋级、不进入外部人工盲评。完整结果见
`docs/EXPERIMENT_G4_3_QUALIFICATION_RESULTS.md`。

## 第38轮：G4.4确定性发言权、注册责任人与真实时限收尾

**目标：** 把G4.3精读发现的四类反例转为跨场景架构不变量：跨角色让渡、可行动责任人、
当前世界证据来源和会议时限语义。

**机制：** 当公开历史中存在指向另一NPC的未答问题时，共享玩家在任何模型调用前直接产生
最小让渡；该公共输入规则对Baseline和RoomMind完全一致。NPC与玩家公开发言不得把会内
阻塞事项交给参与者目录之外的姓名，但显式标记为external/external_followup的会后责任人
仍允许。当前世界命题扩展覆盖posted/shared/reviewed/matched/secured/immutable及复数哈希、
`have just been`等自然表达。达到安全轮次上限时先归约为conditional/deferred，保留全部
未决事项，只有无法归约才使用stalled。新增G4.4确定性探针审计未注册会内责任人。

**本地证据：** 四组无数据库smoke、compileall和diff检查通过；模拟测试证明跨角色问题
无需调用LLM即可让渡。冻结G4.3事故对话重放准确找出Emily Chen与Alex Patel两次未注册
责任人指派和两次无工具依据的现场证据声明，同时保留谈判的两次跨角色违规。随后建立独立
本地PostgreSQL 16测试库，数据库支持的双模式`smoke_modes.py`也通过。G4.4因此通过本地
工程预资格，但仍只是候选：尚未推送、部署或生成新的固定模型配对对话。完整方案见
`docs/EXPERIMENT_G4_4_LOCAL_PREQUALIFICATION.md`。

## 第39轮：G4.4确定性边界生效但总体真实性资格失败

**冻结结果：** 批次`776890ab-6a96-4efc-b777-8d1817ac2a40`在固定
`ollama/gpt-oss:120b`下完成8/8对话和48/48独立AI维度评价，零对话失败、零降级回退，
8个公开转录哈希全部复算一致。两条partial评价只补跑缺失维度，未改动冻结对话。

**机制有效：** RoomMind四条运行全部摆脱`stalled`，三个安全时限结束均保留为
conditional；确定性跨角色让渡触发2次。事故场景拦截1次未注册责任人和6次无依据的当前
世界终态声明，最终RoomMind转录没有遗留对应违规。G4.1至G4.4的发言权、受话人、跨角色
所有权和注册责任人探针全部通过。

**仍然失败：** RoomMind事故场景仍有一组近重复发言，产品发布场景出现确认后撤回、角色
代替、称呼错误和无法形成最终决策。六维AI描述总均值RoomMind为5.083、Baseline为5.250，
差值-0.167；人工逐条精读同样是两对改善、两对退化。新边界显著提高了安全性和可审计性，
但尚未稳定转化为更自然、角色正确且程序完整的多人会议。

**资格结论：** G4.4不晋级、不启动外部人工盲评。后续应以确定性的会议义务图连接角色
权限、证据前置条件、确认/撤回和条件收尾，并把近义重复控制扩展到跨发言者的义务复述。
完整证据见`docs/EXPERIMENT_G4_4_QUALIFICATION_RESULTS.md`。

## 第40轮：G4.5会议义务图治理候选

**目标：** 将G4.4中已经分散存在的字段状态、角色权限、证据边界和结束条件连接为同一个
可审计闭环，重点解决产品发布场景的确认—撤回矛盾、角色代替和跨发言者重复。

**机制：** 每个配置完成条件确定性投影为meeting obligation，记录稳定ID、all/any组语义、
授权确认者、当前缺失确认者、观察值及pending/satisfied/reopened/conditionally_closed状态。
后续授权证据推翻既有确认时显式重开义务；协调器优先路由给真正缺席的授权NPC，而不是泛化
的相关角色。字段级handoff不能把义务交给无对应权限的参与者；普通决策、承诺、行动或证据
陈述的受话人不被误判为责任人。新增跨角色同义义务复述
抑制，但保留授权接受、核验、提交、拒绝和阻塞等
真实进展。独立探针检查义务图、开放义务集合、完成一致性、目标权限和跨角色重复；遥测记录
状态迁移、重开与抑制次数。

**实验边界：** Stanford式独立记忆及perceive–retrieve–plan–reflect–act循环不变，四个场景、
Baseline、模型选择和六维外部评价不变。本轮先进行本地工程回归和本地预实验，未推送、未部署，
不改写G4.4及更早冻结证据。预资格方案见
`docs/EXPERIMENT_G4_5_LOCAL_PREQUALIFICATION.md`。

## 第41轮：G4.5方向性改善但严格资格未通过

**冻结结果：** 批次`e6bac6ed-12eb-4e14-b420-3372e45f3764`完成8/8条配对对话和
48/48项独立AI维度评价，零对话失败、零降级回退，8个公开转录哈希全部复算一致。
RoomMind六维描述均值5.333，Baseline为5.083，差值+0.250；逐条精读认为谈判、产品发布
和领导力面试三对偏向RoomMind，事故指挥一对仍不合格。

**机制有效：** 谈判、发布和面试分别在9、10、12轮满足条件并获得权威闭合锁；事故指挥
在20轮安全时限保留2个开放义务并条件结束，没有虚假标成完成。四条RoomMind运行共记录
13次义务迁移，事故场景抑制3次跨角色义务复述，证明义务图进入真实生成链路。

**仍然失败：** 事故对话在高重试和修复压力下继续出现同一角色重复、相互矛盾的证据状态、
无模拟工具依据的日志/哈希/现场指标声明，并把阻塞事项交给未注册的`Jordan Patel`。谈判
序号11被新离线探针误判为跨角色重复：它实际上包含供应商对84元价格的有效确认，但导出
把带后续提问的整句误判为纯请求，导致accepted intent/material transition未被提交。说明当前既有真实对话缺陷，
也有测量可观测性缺口。

**资格结论：** G4.5不进入外部人工盲评，但保留为G4阶段最强方向性证据。下一轮应是窄幅
修正：把验证意图和工具来源写入冻结导出，让普通输出与repair/fallback输出共享同一命题级
重复和证据边界，禁止未注册会内责任人，并严格区分计划中、执行中和已验证状态。完整证据见
`docs/EXPERIMENT_G4_5_QUALIFICATION_RESULTS.md`。

## 第42轮：G4.6子句证据与恢复路径统一治理

**目标：** 不改变Stanford式独立记忆、义务图或Baseline，只修复G4.5事故场景暴露的
repair/fallback旁路：混合确认被误判、现场证据换一种说法即可绕过、未注册责任人的后置
姓名句式，以及多个语义相同的安全收尾轮换。

**机制：** 公共意图改为子句级落地，允许“我确认X；Y请确认Z”中的第一条有效确认并记录
`material_clause`；新增现场指标、存储桶日志、哈希签名等可见证据的模拟工具来源约束；扩展
“把任务交给Name”和“Name将验证”等会内责任句式；无新证据的玩家恢复只允许一次，第二次
直接真实有界收尾；NPC修复子句和配置回退重新经过同角色与跨角色重复检查。

**本地证据：** 四组无数据库smoke、数据库双模式回归、compileall及两个前端生产构建通过。
冻结G4.5对话确定性重放在谈判、发布、面试中没有新增误杀，在事故指挥中找出8条既有问题
发言；谈判序号11重新落地后保留84元有效确认，并使对应义务重复探针由失败变为通过。

**研究边界：** 重放只证明机制覆盖，不证明新对话更真实。必须部署后新建固定模型8条配对
冻结实验，再独立评价、复算哈希并逐条精读；不自动启动外部人工盲评。完整预资格方案见
`docs/EXPERIMENT_G4_6_LOCAL_PREQUALIFICATION.md`。

## 第43轮：G4.6真实性资格未通过

**冻结结果：** 批次`d0ed05f5-ec64-49dc-8452-e2154d60c2fe`在固定
`ollama/gpt-oss:120b`下完成8/8条配对对话和48/48项独立AI维度评价，零对话失败、零降级
回退，8个公开转录哈希全部复算一致。RoomMind六维描述均值5.667，Baseline为5.708，差值
-0.042；RoomMind在信息边界、时序和流程上提高，但在角色、互动结构和多方互动上下降。

**机制有效：** 产品发布中的混合“确认＋提问”成功提交1条子句级有效确认。事故场景拒绝
10条公共声明，其中7条为无依据的当前世界声明，并修复6条子句、抑制1次义务重复；最终
以4个开放义务真实标记为deferred，没有虚假完成。说明G4.6的子句证据和恢复路径治理确实
进入生成链路。

**仍然失败：** 四条RoomMind中只有领导力面试通过全部适用探针。谈判和产品发布都出现
NPC向另一NPC提问后被玩家抢占发言权；事故指挥重复要求同一角色“直接回答”，并把Security
负责的取证状态错误路由给SRE。逐条精读还发现模板化回退、越权收尾和先称人员已培训后又称
缺两名专家等一致性问题。治理提高了真实性边界，却没有转化为稳定的会议编排优势。

**资格结论：** G4.6不进入外部人工盲评。下一轮应窄幅修正为G4.7：确定性保留NPC-to-NPC
发言权、按权威角色拆分复合问题、同义催促一次后有界defer、清理模板化修复语句，并让后续
公共声明通过既有账本的一致性验证。完整证据见
`docs/EXPERIMENT_G4_6_QUALIFICATION_RESULTS.md`。

## 第44轮：G4.7发言权与权威角色路由治理候选

**目标：** 不改变Stanford式独立记忆、perceive–retrieve–plan–reflect–act循环、义务图、
Baseline或六维评价，只修复G4.6中已经由真实冻结对话证明的会议编排缺口：NPC之间的提问被
玩家抢答、同义“请直接回答”循环、把Security取证事项交给SRE确认、模板化回退，以及角色
明知人员短缺却声称全部就绪。

**机制：** 自主步骤不再丢弃已持久化的`public_intent`、轮次和序号；礼貌引导句中的受话人
会传递给后续问句。玩家对同一NPC只进行一次普通让渡，第二次必须标记有界defer并请求结束，
两个条件都遵循相同的公共玩家规则。任务关键focus的问题只能交给注册`owner_ids`中的角色，
普通跨角色讨论不受影响。NPC公共输出新增高置信自我矛盾检查，只拦截与自身事实约束重叠的
绝对正向断言，不披露隐藏事实。问题式subject在回退前规范化，禁止再次生成
`For Could you...`一类拼接句。

**本地证据：** Python编译、四组核心smoke、PostgreSQL双模式回归和两个前端生产构建通过。
冻结G4.6反事实重放恢复了6处失败案例的正确受话人：谈判2处供应商、发布1处CFO、事故3处
SRE；同时消除模板化回退并拦截“已知缺两名专家却称全部培训完成”的矛盾。新增探针检查
focus权限和重复让渡的有界结束，新增遥测记录这三类治理事件。

**研究边界：** 以上只证明机制覆盖，不能证明G4.7对话比G4.6或Baseline更真实。当前候选尚未
推送、部署或调用在线模型；正式结论必须来自固定`ollama/gpt-oss:120b`的新8条配对冻结批次、
独立六维评价、哈希复算和四对逐条精读。不自动启动外部专家盲评。完整方案见
`docs/EXPERIMENT_G4_7_LOCAL_PREQUALIFICATION.md`。

**线上资格结果：未通过。** 固定版本与模型的8条对话全部冻结，且无对话失败或降级回退；8条
哈希复算和注册探针也全部通过。但独立评价只有47/48维完成，且描述性均值没有显示RoomMind
优势。四对逐条精读中，RoomMind在面试和事故场景更自然、谈判场景不如Baseline、发布场景
各有严重缺陷；仍出现跨角色同义催促循环、截断发言、无工具证据的“附件已收到”、未回答新
问题即停止，以及玩家要求结束后NPC继续质疑。现有探针只覆盖特定元数据事件，未捕获这些
语义层缺陷。评价缺失来自提示允许空证据数组而解析器禁止它的契约矛盾。G4.7不进入外部
人工盲评；下一代应窄幅修正为G4.8。完整结果与冻结证据见
`docs/EXPERIMENT_G4_7_QUALIFICATION_RESULTS.md`。

## 第45轮：G4.8收敛、闭场与证据治理候选

**目标：** 只修复G4.7冻结对话已经证伪的四类机制：新问题被误判为无进展、不同角色围绕同一
事项同义催促、玩家宣布有界结束后NPC继续发言，以及“附件包含/已经收到并审阅”绕过模拟工具
证据边界。Stanford式独立记忆、认知循环、RoomMind义务图、公共玩家和安全型Baseline不变。

**机制：** 显式交给玩家的新问题形成公共待答状态并重置停滞计数；`requested_end`玩家消息落盘
后直接锁定发言权，两种条件都不再开启NPC回合；义务重复检测对当前focus增加短句主题重叠规则；
附件内容和收件确认必须有注册模拟工具结果；称谓式截断片段被拒绝；私有约束冲突从单词重叠提高
到至少两个实质词干。六维评价提示示例改为非空序号，与解析器契约一致。

**本地证据：** 编译、三组纯Python smoke、PostgreSQL双模式及闭场回归通过。新增四项G4.8探针；
冻结G4.7反事实重放能够检测谈判中的`Mr.`和虚构附件、面试中的跨角色重复，以及发布和事故场景
的结束后发言。以上仍只是机制覆盖，不是效果结论；需用固定`ollama/gpt-oss:120b`、seed
`20260908`的新8条配对批次验证。完整方案见`docs/EXPERIMENT_G4_8_LOCAL_PREQUALIFICATION.md`。

**线上资格结果：未通过。** 固定版本和模型的8条对话、48/48项独立评价及8条哈希复算全部
完成，零对话失败和零降级回退。G4.8确实修复了RoomMind的结束后继续发言、无工具依据的实时
附件/收件声明，以及评价提示与解析器的空证据矛盾；谈判场景也以11条消息自然完成，明显优于
78条消息且反复虚构附件的Baseline。

但发布场景在Operations回答后、CFO尚未确认预算时提前有界关闭；面试场景仍重复路由提示、追问
同一证据，并让面试官代替候选人陈述候选人的技术经历；事故场景虽真实保留取证阻塞，却在多个
角色间反复索要同一WORM路径和哈希。RoomMind运行494的同说话人重复探针失败，运行496的非关键
focus和同说话人重复探针失败。AI六维描述均值RoomMind为4.500，Baseline为5.792；人工精读判定
RoomMind赢2对、另2对混合，仍不足以建立稳定真实性优势。G4.8不进入外部人工盲评；下一轮只应
修复多受话人待答义务、路由提示消隐、候选人/面试官事实作者边界和短句跨角色重复。完整结果见
`docs/EXPERIMENT_G4_8_QUALIFICATION_RESULTS.md`。

## 第46轮：G4.9多受话人响应与面试作者边界候选

**目标：** 只修复G4.8冻结证据直接暴露的三类问题：同一玩家消息点名多人后有人未回答便
收尾、重复出现管理式“请从你的领域直接回答”话术，以及面试官代候选人叙述候选人的历史经历。
Stanford式独立记忆与认知循环、RoomMind义务图、公共玩家、Baseline和六维评价均保持不变。

**机制：** 公共请求按子句解析全部受话人，并让直接问题目标优先于礼貌称谢对象和旧待答项；
最新玩家消息中被明确点名的NPC在逐一回答前持续保留为公共待答状态；首次玩家让渡缩短为自然的
“Name, the floor is yours.”；结构化面试中，NPC仍可提问、评价和介绍当前组织，但不能把候选人
经历问题转给另一面试官，也不能以第一人称冒充候选人的历史协作者。所有生成、修复和回退路径
共用同一作者边界。

**本地证据边界：** 新增三项探针。冻结G4.8反事实重放已检出运行494漏掉CFO响应及重复路由
话术，并检出运行496的重复路由和候选人经历作者替代。该重放只证明缺陷能被机制和探针覆盖，
不证明G4.9生成效果。完成数据库与构建回归后，才可用固定`ollama/gpt-oss:120b`、seed
`20260909`建立新的8条配对冻结批次；不自动启动外部人工盲评。完整方案见
`docs/EXPERIMENT_G4_9_LOCAL_PREQUALIFICATION.md`。

**线上资格结果：未通过。** 固定版本、模型和seed的8条对话与48/48项独立评价全部完成，零
对话失败、零降级回退，8条转录哈希全部复算一致。G4.9成功消除了旧的可见路由提示和面试官
冒充候选人历史作者的问题；AI六维描述均值RoomMind为5.000、Baseline为4.708。

但核心多人待答约束仍失败：发布场景一次点名Sales、Finance和Operations后，Finance未在下一条
玩家消息前作答。Finance随后先称预算尚未批准，又在只有无注册工具依据的“已分享材料”后反转
为批准。谈判、发布和面试三条RoomMind运行仍有旧探针失败；事故场景在containment和证据状态未
确认时批准恢复方案，并带着4个未决项提前有界关闭。人工精读判定RoomMind赢谈判和面试、发布
混合、事故由Baseline更自然。G4.9不进入外部人工盲评；下一轮应只修复跨玩家让渡仍保持的多人
响应集合、条件性确认投影，以及面试/事故阶段关闭的单调性。完整结果见
`docs/EXPERIMENT_G4_9_QUALIFICATION_RESULTS.md`。

## 第47轮：G4.10前置条件与终局发言权治理候选

**目标：** 只修复G4.9冻结证据中的三处提交边界缺陷：玩家可见让渡错误切断原始多人待答集合、
带`provided/pending`等条件词的确认被提交为接受，以及后阶段批准或同轮后续NPC在前置状态未满足
或最终确认后继续发言。Stanford式独立记忆、RoomMind义务图和公共账本、共享玩家、Baseline及
六维评价协议保持不变。

**机制：** 多人待答集合跨确定性“Name, the floor is yours”让渡保留，直到所有原始受话人发言
或出现新的实质玩家请求；`accepted/verified`在最终公开引文边界按子句排除条件、否定和待定措辞；
后阶段字段接受前检查配置中的阶段入口前置条件；每个NPC公共账本事件提交后立即确定性投影任务
状态，若满足全部完成字段则锁定同轮剩余发言权。

**本地证据边界：** 新增条件确认和终局同轮发言两项探针，并修正多人待答探针对可见让渡的
边界解释。冻结G4.9反事实重放不再把运行502的Dana让渡误判为丢失响应，但仍检出其条件性预算
接受；同时检出运行504最终确认后的继续发言。该重放只证明缺陷覆盖。通过完整本地回归后，才可
使用固定`ollama/gpt-oss:120b`、seed `20260910`建立新的8条配对冻结批次；不自动启动外部人工
盲评。完整方案见`docs/EXPERIMENT_G4_10_LOCAL_PREQUALIFICATION.md`。

**本地回归结果：通过。** Python编译、speech-safety、LLM韧性、公共账本、研究协议、冻结G4.8/
G4.9行为重放、PostgreSQL双模式回归以及client/admin生产构建均通过；前端仅保留既有大chunk
警告。候选代码具备建立新固定模型预发批次的条件，但尚无G4.10真实生成效果证据。

**线上资格结果：未通过。** 正式批次`6394089e-662e-422b-9b82-9bea10264650`在固定版本、模型和
seed下完成8/8条冻结对话、48/48项独立评价及全部哈希复算，零对话失败和零降级输出。G4.10新增
的条件确认拒绝、可见让渡后的多人响应集合、终局同轮锁三项探针均通过，说明窄机制按设计工作。

但RoomMind在六个AI维度全部低于Baseline，描述性总均值4.625对5.833；人工逐对精读仅赢冗长的
谈判Baseline，发布、面试和事故三对均由Baseline更自然。发布和事故场景因NPC对NPC问题经玩家
让渡后没有得到目标角色响应而停滞；面试在证据和候选问题已公开完成后重新开启第二段案例并跑到
20轮安全上限；事故公开措辞在证据前仍称恢复方案已批准，尽管账本没有提交该批准。运行518另有
同说话人重复，运行520有无模拟工具依据的当前世界完成声明。旧G4.3探针对新的简短让渡产生三项
假阳性，但即使排除它们，严格探针门仍失败。G4.10不进入外部人工盲评；下一代应把已解决状态、
义务图和发言调度原子化，并以NPC直达调度取代可见的玩家行政让渡。完整结果见
`docs/EXPERIMENT_G4_10_QUALIFICATION_RESULTS.md`。

## 第48轮：G4.11直达路由与状态—措辞收敛候选

**目标：** 只修复G4.10冻结证据中的三类控制层分叉：NPC向另一NPC提问后由玩家充当行政中转、
权威账本已经接受字段但旧变量快照仍让义务和focus重新开启，以及结构层拒绝的确认仍以第一人称
批准措辞公开出现。Stanford式独立记忆、RoomMind公共账本和义务图、共享玩家、Baseline、四个
场景和六维评价协议保持不变。

**机制：** 可见的NPC定向问题在同一自主回合为目标保留紧邻响应槽；若目标已发言则允许一次有界
补充tick，每回合至多新增一条直达响应边，避免无限代理链。选择focus前，从同一权威公开账本
依次投影工作项和字段、计算完成条件并重建义务图；只有带公开授权事件的真实反对才能重新开启。
当`commit_allowed=false`时，公开措辞不得以“I approve / I confirm / I sign off”等第一人称语言
重新引入被拒绝的状态跃迁。

**本地证据边界：** 新增直达同轮响应及拒绝状态不得通过措辞复活两项探针。冻结G4.10反事实
重放检出运行516、518、522的玩家中转缺答，并检出运行516的拒绝状态公开确认。该重放只证明
机制和探针覆盖，不证明G4.11真实生成效果。完整门槛见
`docs/EXPERIMENT_G4_11_LOCAL_PREQUALIFICATION.md`。

**本地回归结果：通过。** Python编译、公共账本、speech-safety、LLM韧性、研究协议、冻结G4.6
至G4.10行为重放、PostgreSQL双模式与闭场回归、client/admin生产构建全部通过；前端仅保留既有
大chunk警告。候选具备建立新固定模型预发批次的工程条件，但尚无G4.11真实生成效果证据。

**预发资格结论：失败。** 固定生成修订`496bd1058432d7f5389ef3898fbac58e686c5b3b`、
`ollama/gpt-oss:120b`与seed `20260911`完成8/8条冻结对话，零对话失败、零降级输出，全部转录
哈希复算一致。G4.11的NPC到NPC同轮直达探针在四条RoomMind运行中均通过，但四条RoomMind
运行全部违反“被拒绝迁移不得以更强公开措辞复活”，运行528还提交了一条条件确认。六维AI均值
全部落后Baseline，逐对人工阅读为Baseline 4胜、RoomMind 0胜；RoomMind仍出现玩家行政让渡、
错误或缺失目标发言人、无工具依据的实时制品声明和带未决义务的条件闭场。

独立评价首次仅完成46/48项；运行527、528的角色策略维度反复返回总分存在但子指标为空的JSON。
经用户授权，评价器专用修订`dc159780997b3b160641c02f22b09e521f9766e2`加入带失败反馈的有界
修复提示，只补算两项缺失维度，其余已完成维度和全部转录哈希保持不变。该分离修订已在审计记录
中披露，也意味着本批不是单一修订的端到端评价运行。G4.11不进入外部人工盲评。完整结果见
`docs/EXPERIMENT_G4_11_QUALIFICATION_RESULTS.md`。

## 第49轮：G4.12玩家响应锁与状态—措辞原子边界候选

**目标：** 只修复G4.11冻结证据中仍未闭合的三条链路：玩家点名或明确让渡后错误角色抢占发言、
结构迁移在最终引文校验中被拒绝但更强确认措辞已经公开，以及“已放进共享文件夹/网盘/仓库”类
实时制品声明绕过附件关键词防线。Stanford式独立记忆、RoomMind公共账本和义务图、共享玩家、
Baseline、四个场景和六维评价协议保持不变。

**机制：** 从玩家公开话语解析出的注册受话人形成按出现顺序的响应锁；锁未清空前，普通mention、
dispatch规则和focus不能让无关角色占用发言额度。决策草稿在渲染前做一次引文落地，最终渲染结果
在写入时间线、记忆或账本前再次按落地后的生命周期校验；不安全内容只能保留独立安全子句，否则
静默抑制。制品检测扩展至plan、roadmap、spec、folder、drive、repo等当前完成/可用性措辞，同时
保留真实的未来承诺。

**本地证据边界：** 新增玩家受话响应锁探针；冻结G4.11反事实重放检出运行526、528、530的错误
或缺失受话人、四条RoomMind运行的拒绝迁移措辞复活，以及运行528四处无工具依据的共享空间制品
声明。该重放只证明缺陷覆盖；只有完整本地回归通过后，候选才具备使用固定
`ollama/gpt-oss:120b`、seed `20260912`建立新预发批次的条件。完整门槛见
`docs/EXPERIMENT_G4_12_LOCAL_PREQUALIFICATION.md`。

**本地回归结果：通过。** Python编译、公共账本、speech-safety、LLM韧性、研究协议、冻结G4.6
至G4.11行为重放、PostgreSQL双模式回归以及client/admin生产构建全部通过；前端仅保留既有大
chunk警告。候选代码具备建立新固定模型预发批次的条件，但尚无G4.12真实生成效果证据。

**预发资格结论：失败。** 固定修订`433c179408195243a5d5d219db28903a10b08e57`、
`ollama/gpt-oss:120b`与seed `20260912`完成8/8条冻结对话，零对话失败、零降级输出，全部转录
哈希独立复算一致。独立评价首次完成47/48项；运行534的`procedural_fidelity`因截断JSON被
拒绝，随后仅补算该缺失项并达到48/48，既有分数与全部转录哈希未改变。

G4.12的拒绝迁移—公开措辞原子边界在四条RoomMind运行中通过，但玩家受话响应锁未收敛：运行
532、534、536仍有错误、缺失或延迟受话人；运行532、538违反继承的NPC同轮直达门槛，运行534
在终局确认后继续发言，运行536出现无工具证据的当前制品声明。因此四条RoomMind运行全部至少
违反一项适用探针。六维AI总体均值RoomMind为5.125、Baseline为5.417；人工逐对阅读为
RoomMind两场窄胜、Baseline一场胜、一场无胜者，不构成稳定优势。G4.12不进入外部人工盲评。
完整结果见`docs/EXPERIMENT_G4_12_QUALIFICATION_RESULTS.md`。

## 第50轮：G4.13必答响应与终态收敛候选

**目标：** 只修复G4.12冻结证据中的必答角色静默、多人受话顺序、NPC直达响应、终态后继续生成
玩家发言，以及礼貌确认和当前世界制品声明绕过最终发布边界的问题。共享玩家、四个固定场景、
Baseline、模型、seed策略和六维评价协议保持不变。

**机制：** 每个显式受话人获得有序必答槽；正常渲染不能安全发布时，仅对该响应所有者尝试经过
同一发布校验的角色局部确定性回退。NPC到NPC问题使用相同必答约束，未解决所有权保持排队。
自主玩家步骤开始前若任务已经终态则立即锁定发言权；拒绝迁移扩展到礼貌确认构式，并继续对
玩家产生的当前世界声明执行证据边界。

**本地回归结果：通过。** 全部服务端烟雾测试、PostgreSQL双模式集成、client/admin生产构建及
冻结G4.13行为重放均通过。授权修订`87688345d961b8d0e834d34bd5498c37783e50b2`随后部署到
预发，并以固定`ollama/gpt-oss:120b`和seed `20260913`建立资格批次。

**预发资格结论：失败。** 批次`335f6406-5cd2-4028-a5ac-9803a4b8bd9a`完成8/8条冻结对话、
48/48项独立评价，零对话失败、零降级输出，全部转录哈希复算一致。运行540通过所有适用探针，
使RoomMind清洁通过率由G4.12的0/4改善到1/4；但运行542、544仍丢失玩家明确点名的目标响应，
运行546的NPC直达提问仍被玩家行政让渡打断，因此三条运行的G4.13收敛探针失败。

六维AI描述均值RoomMind为4.750、Baseline为5.167，差值-0.417；RoomMind仅赢1/4个AI配对。
人工逐对精读为RoomMind赢谈判与面试、Baseline赢发布与事故，未形成稳定优势。面试与事故中还
可见没有工具证据却声称已上传、审阅或归档实时制品的措辞，说明探针覆盖仍需加强。G4.13不进入
外部人工盲评；下一代应把待答所有者提升为发布边界的不变量，并补充文本制品完成声明的检测。
完整结果见`docs/EXPERIMENT_G4_13_QUALIFICATION_RESULTS.md`。

## 第51轮：G4.14发布所有者与实时制品证据治理候选

**目标：** 只修复 G4.13 冻结证据中仍未闭合的三条链路：结构化受话人信息没有进入当前玩家
消息的调度、NPC 直达调度只有一个全局补充槽而不能完成 A→B→C 的有界链，以及实时制品完成
声明的文本覆盖不足。其余实验条件和研究架构保持不变。

**机制：** 当前公开玩家消息的 `public_intent.target_id` 成为文本解析的补充事实；明确受话人的
`required_response` 一直传递到最终发布边界，模型输出与上下文回退都失败时仅发布不带状态迁移
的角色局部答复。NPC 问题链的预算提高到在场角色数，但同一目标每轮只能被追加一次。证据边界
新增对已上传材料的审阅、Confluence/仓库可用性、日志或快照复制到归档桶、manifest 已生成等
现在时完成声明的拒绝。

**本地回归结果：通过。** G4.14 冻结 G4.13 反事实重放保留运行 540 的清洁通过，并精确检出
运行 542/544/546 的响应所有者失败和运行 544/546 的制品证据失败。Python 编译、公共账本、
speech-safety、LLM 韧性、研究协议、冻结 G4.6 至 G4.13 重放、PostgreSQL 双模式与闭场回归，
以及 client/admin 生产构建均通过；前端仅保留既有大 chunk 警告。本结果只证明机制覆盖，不证明
真实生成效果或对 Baseline 的优势，也不授权推送、部署或启动在线批次。完整门槛见
`docs/EXPERIMENT_G4_14_LOCAL_PREQUALIFICATION.md`。

**在线资格结果：严格失败。** 固定批次完成8/8对话与48/48评价，哈希全部一致，且4/4
RoomMind运行通过全部记录探针；然而AI均值差为-0.938，RoomMind没有赢得任何配对。人工精读
进一步发现运行550的无证据就绪反转、运行552的虚构制品审阅及终态重开、运行554的未注册
事故所有者和跨角色代行动作，以及运行548的模板化/畸形回退话术。G4.14暴露出显著的探针
过拟合风险：结构检查完全通过，但真实语义质量反而退步。下一轮应改为类型化发布声明与最终
发言统一验证，而不是继续扩展短语列表。完整结果见
`docs/EXPERIMENT_G4_14_QUALIFICATION_RESULTS.md`。

## 第52轮：G4.15类型化发布声明与终态阶段治理候选

**目标：** 修复G4.14“探针全部通过但人工语义失败”的覆盖缺口，不再把新增安全边界实现为
互不关联的短语补丁。重点统一最终发言中的行为主体、当前世界动作、制品证据、公开所有者与
任务阶段。

**机制：** 新增不可变的`PublicationClaim`类型，从最终公开子句抽取actor、claim kind、object、
predicate、temporal scope、evidence source和authority scope，并在引文落地之后执行同一发布
验证。当前运营就绪、实时动作和本次会话制品审阅必须具有注册工具证据；角色不能替另一角色
承诺运营动作；公开所有者必须在参与者目录中。面试一旦进入候选问答阶段，后续面板问题不得
重新索取候选经历。最后兜底话术移除角色标签和编排措辞，并新增合成回退诊断。

**本地回归结果：通过。** Python编译、公共账本、speech-safety、LLM韧性、研究协议、
PostgreSQL双模式集成、冻结G4.6至G4.14行为回放以及client/admin生产构建均通过；前端仅有
既有大chunk警告。新的冻结回放使G4.14四条RoomMind运行全部按预期失败，并精确标出运行550
的无证据就绪声明、运行552的制品审阅和阶段重开、运行554的实时动作与未注册所有者、运行548
的合成回退。完整门槛见`docs/EXPERIMENT_G4_15_LOCAL_PREQUALIFICATION.md`。本地通过不授权
推送、部署或启动在线资格批次。

**在线资格结果：严格失败。** 固定批次完成8/8对话、48/48评价及全部哈希复算，但只有1/4
RoomMind运行通过适用探针，AI均值落后Baseline 0.713。人工精读确认类型化发布层仍漏掉当前
产能/附件声明，必答所有权与多受话人响应链继续失配，面试终态后仍可出现发言，安全回退在
事故场景中退化为重复低信息请求。G4.15没有形成稳定优势，不进入外部人工评审。完整结果见
`docs/EXPERIMENT_G4_15_QUALIFICATION_RESULTS.md`。

## 第53轮：G5因子拆分、评分测量与全新家族验证

G4系列不断增加规则却未形成稳定优势，G5因此不再把所有机制合并成单一RoomMind条件，而采用
认知开关×治理开关的2×2完整因子设计，并把评分器校准从架构效果估计中拆开。恢复的旧材料八个
家族全部已有开发暴露，不能诚实充当留出集；新框架在生成前冻结四个全新家族、每家族两个世界，
以及每世界一个玩家和三个NPC的条件中立角色包。

旧G5清单只能绑定一份共享角色输入，不能表达不同场景各自的角色表。基础设施现增加可选的逐场景
角色哈希映射，并以映射摘要作为四臂共同输入；旧清单仍按原路径重建。评分器开发验证先只生成
8条、四臂各2条的盲化对话，明确禁止据此估计架构效应；通过评分门后才建立32条完整区组筛查。
冻结计划摘要为`cc1d4fd9f89173fd2936e583a995d31e15143100e2f659911114de97b54357ec`。
计划的源码修订和模型绑定仍为空，未授权执行、外发、推送或部署。

随后增加显式在线路由装配边界：只有调用方注入、且公开运行规范与冻结配置完全一致的Ollama
传输对象才能重建组件；密钥和URL不写入清单，装配本身不发请求，原离线装配器仍拒绝网络模型。
共同请求/响应字节预算可逐层重建，Ollama聊天传输不能被用作嵌入传输。该工程能力不改变计划的
`launch_authorized=false`。

生成前方法学复核又否决了未执行的v1新材料：其核心事实过度公开、角色目标过度通用，不足以测量
认识论与策略差异。v1完整保留。v2为每个世界新增三项角色可见且可披露事实、一项角色可见但受
保护事实，并给每个角色独立目标；框架、角色包与平衡验证计划摘要分别为`a7d0bfa…a648e`、
`7bae19f2…ff53`、`6070c945…c2fd`。v2仍未执行或外发。

用户随后为v2单独授权提交、推送、预发部署和8条固定`ollama/gpt-oss:120b`开发对话。为避免
篡改预生成记录，原frame/role/plan的未授权字段保持不变；新增独立执行绑定，记录精确源码revision、
逻辑路由、模型、预算、停止规则、授权ID及从完整manifest中选出的8个一世界一臂分配。执行器用
同一可恢复SQLite事件库逐条运行，每条立即封存内部source，全部完成后才生成公共转录；它没有
48项评分、32条筛选或确认研究入口。提交前专项通过，完整验收390项（含48项PostgreSQL）无失败、
错误或跳过；受沙箱限制的第一次342/390环境失败回执也保留，不覆盖。在线结果尚未产生，因此
本条记录不预判评分器有效性或架构优势。

## 第54轮：G5新家族v2执行失败与v3版本化恢复

G5 v2在线执行在第一世界提交14个公开事件后反复停在结构化注释调用。三次进程级恢复均保留
同一执行绑定、SQLite和尝试历史，最终累计232次模型调用尝试、0条冻结source。隔离副本的
响应形态探针确认Ollama返回HTTP 200，但`gpt-oss:120b`把2048个生成token全部用于thinking，
公开content为空且`done_reason=length`；传输层按冻结契约拒绝该响应是正确行为。

隔离副本绑定`think=low`后成功封存第一条16轮对话，又暴露装配器把全部assignment映射到同一
`assembly-validation` world ID的批次冲突。v3因此同时版本化两项执行修复：Ollama HTTP v2
规范显式哈希绑定低推理强度；world ID由manifest摘要和assignment ordinal确定性派生。v3还
绑定失败前驱的执行摘要，使用新目录运行，不修改或冒充续跑v2。完整本地验收393项（含
PostgreSQL）全部通过，无失败、错误或跳过。详细证据见
`docs/G5_FRESH_EXECUTION_V2_FAILURE_AND_V3_RECOVERY.md`。该修复仍只授权8条评分器开发对话，
不授权48项评分、32条架构筛查、确认研究或外部人工评审。

## 第55轮：G5新家族v3计划证据失败与v4有界结构修复

v3在全新目录中证明前两项修复有效：第一条A臂对话完成16轮并封存，第二assignment建立了独立
world。随后第二世界在首个事件提交前连续生成缺少`source_ids`的层级计划，被既有严格验证器以
`Plan source required`拒绝。v3停止时保留2个world、16个事件、152次模型尝试和1个freeze；
全部绑定、SQLite、日志和source经加密SSH下载并哈希归档。没有把隔离探针或失败部分冒充完整批次。

v4不采用任意补入证据ID的做法，而新增哈希绑定的`g5-plan-validation-feedback-v1`：验证失败时最多
两次把固定错误反馈给同一模型重新生成，候选仍须只引用`memory.retrieved`中的真实ID并通过完整
验证；被拒候选的响应摘要与错误进入认知收据。v4完整本地验收395项（含PostgreSQL）全部通过，
零失败、错误或跳过。详细证据见`docs/G5_FRESH_EXECUTION_V3_FAILURE_AND_V4_RECOVERY.md`。

## 第56轮：G5新家族v4问题标注失败与v5有界标注修复

v4证明计划修复有效并连续封存前三条16轮对话；第四世界`research-data-release-v2`的第16次
提交前，模型问题标注响应把公开问题关联到无效targets，严格投影以`Invalid question targets`
拒绝，未提交无效事件。进程停止时保留4个独立world、63个事件、860次模型尝试、3个freeze和
3份可验证source；全部经加密SSH下载并核对哈希，SQLite完整。失败原始标注文本没有持久化，
只能确认响应哈希、字节数和确定性验证错误，不能倒推具体无效值。

v5保留v4的计划修复，并新增所有四臂共享、哈希绑定的
`g5-question-validation-feedback-v1`。问题标注在提交前对最终公开发言、注册参与者和既有问题
投影验证；失败时最多两次把固定错误反馈给同一模型，不能改写公开发言。成功修复时保留被拒
请求/响应哈希与错误，全部失败则不提交事件。离线装配器按冻结规格恢复修订上限。完整本地验收
400项（含PostgreSQL）全部通过，零失败、错误或跳过。详细证据见
`docs/G5_FRESH_EXECUTION_V4_FAILURE_AND_V5_RECOVERY.md`。v5尚未获推送、部署或外发授权。

## 第57轮：G5新家族v5计划状态证据失败与统一结构输出审查

v5在新绑定和新目录中证明v4的问题标注修复能够支持前三条16轮对话封存；第四世界
`research-data-release-v2`的第5个事件提交前，`principal_investigator`连续生成缺少
`status_source_ids`的计划状态跃迁。已有两次有界计划修订仍未满足验证器，三个进程级尝试
均在同一world version以`Task transition needs observed evidence`停止。冻结状态保留4个world、
52个已提交事件、560条尝试记录和3份source；错误事件未提交，SQLite完整，远端与本地文件
哈希一致，三份source的事件链和最终seal均通过独立验证。

该失败说明v4/v5的逐接口补丁尚未形成可靠的研究生成仪器。当前六类模型结构输出只有计划和
问题标注具有有界修订，且失败日志只保存响应哈希和字节数，不保存被拒结构及字段路径。下一版本
前应先建立共同的严格结构生成层、机器可读验证反馈、内部拒绝响应胶囊、跨进程修订预算和覆盖
全部接口的固定模型资格阶梯，不直接追加新的单点提示补丁。详细记录见
`docs/G5_V5_FAILURE_AND_STRUCTURED_OUTPUT_REVIEW.md`。v5仍是失败开发证据，不得评分、拼接或
用于架构效应估计。

## 第58轮：G5统一结构输出层与v6本地恢复候选

针对v5暴露出的系统性缺口，本轮不再只修改`status_source_ids`提示，而建立
`g5-validated-structured-generation-v1`共同协议。策略决策、反思、层级计划、治理审计、会话
标注和问题标注六类结构输出现在共享有界修订、机器可读错误、字段路径、约束、不允许杜撰的
候选ID集合及内部拒绝响应胶囊。解析、信封、引用、跨度、状态跃迁和权限错误可区分；每个被拒
响应保存请求/响应摘要和原始文本。修订成功才继续提交，耗尽则把完整胶囊写入内部attempt
journal，不产生公开事件。

v6绑定把全部结构接口的修订上限统一冻结为2。执行器将终止性`StructuredOutputError`从网络
瞬态重试中剥离，因此进程重启不能重置已经耗尽的结构修订额度。装配器同时保留v2至v5旧版
计划/问题修订字段的解析路径，既有归档仍以各自精确源码修订为准。针对v5原故障新增确定性回归：第一次返回`completed`且
`status_source_ids=[]`，第二次收到精确字段和允许证据ID后才通过。Python编译及完整本地G5
测试399项通过，48项依赖本机PostgreSQL服务的测试跳过；另16项相关server测试在依赖完整的
项目Python环境中通过。未联系模型提供商、未部署、未启动新实验。

## 第59轮：G5 v6-v7实时资格迭代与工程门通过

统一结构层先通过六接口实时检查，但四臂长运行依次暴露 runner 字段错误、任务身份和终态混淆、
外层修订编号、旧回执完成新任务、反馈说明字段被复制进任务输出，以及父目标未随子任务同步等
问题。每个失败批次均保留独立绑定、SQLite、日志和内部拒绝胶囊；修复只增加精确、机器可读的
验证反馈，没有放松来源边界或任务完成标准。

最终修订`f5cd0051aaf761cba4f4154719e510e9379db5ad`通过六接口实时资格和四臂长运行资格。A-D每臂
16个事件，共388条尝试和4个freeze；累计第4事件后的故意进程退出由同一绑定和SQLite恢复。
独立复核确认数据库完整、binding与summary哈希有效、四份source全部通过验证。最终本地G5回归
403项通过，48项PostgreSQL测试因本机无服务跳过。完整记录见
`docs/G5_V6_V7_ENGINEERING_QUALIFICATION_20260912.md`。资格产物仅是合成工程证据；8条评分器开发
验证、32条架构筛查和确认性研究仍未启动。

回归期间本机macOS还记录了ONNX Runtime在`PosixTelemetry::Shutdown`退出清理阶段的子进程
`SIGABRT`。它未发生在G5提交路径，且远端资格产物已独立复核，因此不撤销资格结果；但本机
清洁退出门应记为未通过，后续须在初始化前关闭ORT遥测并以零新增崩溃报告复验。
