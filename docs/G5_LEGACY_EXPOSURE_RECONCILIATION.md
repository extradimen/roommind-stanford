# G5 历史材料与暴露核对（2026-09-12）

这是本地历史库存/来源检查，不重新评价旧实验，不调整旧分数或旧资格结论。
没有精读全部历史对话，因此不把此次元数据审计称为语义复评。

## 范围与结果

直接遍历本仓库research/experiments，不依赖Git忽略规则。69个非G5历史文件、63份
EXPERIMENT_G开头的协议/结果文档已记录字节摘要。覆盖16份转录导出、128个运行，
包括G1主探索、四份早期双运行模型试验、G4.6至G4.15和G4.16筛选。
文件数、导出数、运行数不是独立实验家族数。

- 126个运行的公共转录canonical JSON摘要与原记录完全匹配。
- 2个运行没有记录摘要：old-deepseek的run 4及old-gpt-oss的run 6，均dialogue_failed。
  现在重算了可见消息摘要，但不能补造它们当时已冻结/已通过的证据。
- 16份导出的manifest内容摘要均匹配，签名/部署真实性未认证。
- G4.16包含4份完整frozen_inputs，各自摘要及聚合摘要均匹配。
  其余导出未提供该字段，记unknown，不推断角色/调度输入完全相同。
- 使用旧 `research_protocol.transcript_provenance` 独立核对128个重算结果，全相同。
- 原交接包对当前research目录116文件的核验仍通过，旧文件未改写。

完整格式清单：`G5_LEGACY_LOCAL_CENSUS_20260912_COMPLETE_FORMATS.json`，内容摘要
`b7b64355b8959bb7f7eb62c8192306129101ffc4332545777b6cff3c5edc4aa7`。
初次清单只解析未压缩JSON，漏解析7份gzip转录（文件摘要仍列入），保留初次清单
`G5_LEGACY_LOCAL_CENSUS_20260912.json`，不得引用其9导出数字冒充完整格式结果。

只读复现脚本：`scripts/g5_history_inventory.py`，本次字节摘要
`09f411dde3e1b16e00d20cbc6a2f813afebc67ead0187fc21e39944711eb119e`。
脚本输出独占新文件，不修改旧文件，连续两次库存结果相同才发布。它不属于350项G5
回归回执的源码集合；本次验证是128项旧算法对照、双遍库存与原交接复验，未重跑无关
数据库全套测试，不借旧回执声称新脚本被全量测试覆盖。

## 保守家族暴露处理

下列配对是**待纳入正式登记的保守排除关系**，不是证明所有文本语义完全相同。
证据是导出的slug/代际及G4.16结果文档明确说明反复检查的开发场景；仅增加world-v3
后缀不构成未见家族。此表不自动写入任何已冻结registry或manifest。

| 排除家族 | 原slug | 后续变体 | 处理 |
|---|---|---|---|
| 供应链谈判 | supply-chain-negotiation | supply-chain-negotiation-world-v3 | 已暴露，不能作为G5未见确认家族 |
| 产品上市决策 | market-launch-go-no-go | market-launch-go-no-go-world-v3 | 已暴露，不能作为G5未见确认家族 |
| 候选人面试 | candidate-panel-interview | candidate-panel-interview-world-v3 | 已暴露，不能作为G5未见确认家族 |
| 事故指挥 | incident-response-command | incident-response-command-world-v3 | 已暴露，不能作为G5未见确认家族 |

原slug在G1主探索、G4.6–G4.15已出现；world-v3变体在G4.16筛选出现。
这些材料仍可用于开发反例、来源审计、单独标识的校准材料，但不能混入确认集或假装
重新换seed就消除开发暴露。不能把旧Baseline得分直接与G5新协议分数作因果比较。
旧config/research-generations.json中的历史资格字段不改变上述G5使用边界，原配置保留。

## 仍未核实的缺口

本仓库research目录没有发现G2系列、G3系列、G4.0–G4.5对应的完整转录导出。
本地有相关协议/结果文档，不等于原始数据已找到，也不证明原始数据被删除。
此次未登录服务器、未扫描整个用户目录、未读取数据库/凭据或下载任何新数据。
外层工作区的普通文件搜索仅作定位线索，不属于此清单的完整审计范围。

完整历史门仍未通过：须定位这些代际的既有归档，核对批次ID、原始转录/评价/摘要与
结论文件；然后将实际材料按稳定家族/父变体/暴露证据加入**新的**G5登记，不改旧冻结表。
缺失位置、未知输入、失败运行必须保留。新增家族还需独立的语义新颖性审查与研究冻结。

W0新增可验证本地历史库存及四类保守暴露关系；W1–W7既有工程门不变，W8未启动。
真实模型/真人校准、正式抽样计划及上线授权仍未完成。

## 后续定位及可执行排除登记

继续检查了本地Git全部可达refs的research路径历史、/private/tmp以及当前用户临时目录
中两层以内的RoomMind/G3/G4.5命名线索，仍未定位G2/G3/G4.0–G4.5原始导出。
发现的临时G4.12文件不是缺失代际；未读取无关个人文件，也未登录服务器。
更正先前口头推测：旧资格工件实际由Git跟踪，不能把普通rg列表漏列归因于Git忽略。
此次直接目录遍历包含gzip，不受那次搜索表达式/展示范围影响。

已建立独立新登记 `docs/G5_LEGACY_REGISTRY_20260912/registry.sqlite`，原冻结登记未动。
8个slug按4个父/变体组连接、4个G4.16完整输入摘要、8条开发暴露声明，共20事件。
声明者明确为assistant元数据审计，不伪称原实验操作者/真人评审，也不认证语义同一性。
JSON导出摘要 `2e2143cec59998e5af61c750a702c4bbb6fb92dcaa6640771af8d7d4ae27e666`。
排除检查摘要 `c0ece43feebf5086e9ae6ad1b119adb0f6d82bfae9f1cc09c27a935bf8db6142`。
8个选择均关联暴露事件，registered_exposure_free=false，不能成为未见确认样本。

实际库重连与20事件幂等重放不改变导出；7项现有登记专项回归通过，原116文件交接
仍通过。未改变运行接口，因此未重复数据库全套验收；新辅助脚本不在350项回执范围。
复现入口 `scripts/g5_register_legacy_exposure.py` 要求全新输出目录，核对原工件摘要
后才生成登记。它只有保守排除用途，不是完整新研究registry或自动选样器。

剩余历史缺口需要旧归档位置或服务器只读原始导出核实；不能用结论文档补造原始数据。
W0/W1新增已知暴露的执行拦截证据，其余W2–W8状态不变。

## 用户授权后的服务器补取

用户明确允许本次只读SSH查找/下载。现已补存17份既有服务器临时归档和18个文档标识
旧批次的54份当前GET导出，71文件传输摘要匹配。18批/144运行原转录摘要及manifest
摘要全匹配；6组旧/新转录运行ID与公共摘要一致。无效G4.2另存3份audit-only导出。
目录 `research/experiments/2026-09-12-g5-legacy-server-recovery/`；审计摘要
`9bdc4fa466b59cf9c5fff579ab865b8a280801dbcbf6479f8da108d990bb6125`。
已补齐文档指向的G2–G2.3、G3.2–G3.9、G4.0/G4.1/G4.2.1/G4.3–G4.5资格批次来源。
G3/G3.1历史主要是本地预资格/回放/压力测试，未建立各自服务器资格批次证据，不能
编造对应批次。其他本地压力/试运行全历史仍未声称齐全。新导出不是当时文件字节，
当前debug探针也不是当时版本探针；未重新评分或精读全部对话。新增材料仍属于四个
已暴露家族，不改变确认集排除；既有登记保持原样。无部署/重启/模型调用。
