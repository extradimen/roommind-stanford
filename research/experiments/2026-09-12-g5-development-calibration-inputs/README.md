# G5 开发校准首批材料（已授权，尚未调用）

用户2026-09-12明确授权Mac客户端调用Ollama Cloud/gpt-oss:120b开展开发校准，
不包含推送、部署、真人邀请或正式确认研究。当前阻塞是本机实际凭据缺失。

`inputs.json` 固定选取完整G4.4旧批次的8条公共对话，四家族、两条件，不按评分或
错误挑选；每条6维，共48预测任务。条件/家族/来源映射仅保留在本地case元数据，
请求只含量规、角色名/权限及全文公共消息。没有隐藏草稿、原评分或参考标签。
原消息未截断；输入最多约37KB。不得把48任务称48独立样本。

输入摘要：`34d65df68c09ab21ecab04c6a088817505a3bebcdc3532def9101a7ff5e4d92d`。
固定温度0、输出上限4096、单并发、每例最多2次技术尝试，最多96请求；完成/弃权
不重跑，认证或模型不一致停止。原始HTTP响应仍须由执行入口保存，当前没有执行入口
运行，真实模型调用0。准备脚本没有访问网络或读取凭据。

这是首次自然文本预测/格式与覆盖检查材料，不是完整准确率校准：参考标签均为空，
不能计算误报/漏报或声称量表可靠。无完整私有事实和权威执行日志时应允许abstain；
不能仅因文本未提供证据就断言世界事实为假。后续真实参考标注/语义检索/意图保持
仍需另行完成，不由这批旧文本预测替代；旧数据不能证明G5行为改善。

本机OLLAMA_API_KEY、OLLAMA_CLOUD_API_KEY均未设置，项目.env及server/.env不存在，
platform.json实际apiKey为空，apiKeyConfigured标记不等于可用密钥。未读取/复制服务器
密钥。下一步需要本机安全配置凭据，或用户明确允许SSH只读将既有服务器凭据送入
本次客户端内存（不显示、不落盘）。取得凭据后先实现并验证持久原始响应/断点执行入口，
再按冻结计划运行；不把授权已收到误报为模型已开始运行。
