# G5 按案例校准桥接合成工件

完整 `panel.json` 包含源绑定标注归档、固定预测者声明、按先后顺序保存的预测尝试和
逐案例/家族/维度/类别/标签来源报告。外部模型调用0、真人标注0；仅合成会计/审计验证。

内容摘要（移除自身sha256后的canonical JSON）：
`3911fabbeca5760c05e4ceb595aca0351e1b4a605c4db330f1e3aa51c57b26d4`。

| 统计轴 | 已知夹具值 |
|---|---|
| 案例/源对话 | 24 / 4，标签记录不当新增案例；六维共享源，不独立 |
| 参考标签 | 2未标齐、1未裁决分歧、1不确定、1不适用，其余明确 |
| 最终预测 | 4缺失、4技术失败、16完成；完成中4弃权 |
| 全部失败历史 | 8次，包括后来成功的重试 |
| 明确参考/预测配对 | 10/24，覆盖率41.67% |
| 明确配对混淆矩阵 | TP=3、TN=7、FP=0、FN=0，完全由脚本设定 |

即使夹具明确配对FPR/FNR为0，也只有10/24进入该分母，绝不代表系统误报/漏报为0或
资格通过。报告区分参考和预测两个交叉轴，不能把两个轴的计数相加成案例总数。

`calibration_bridge.report(annotation, results)` 会重验全部源/标注/裁决、原始预测任务、
预测者绑定、失败重试前置引用和结果链，再重算全部计数。不直接迭代resolved_label_records，
故同例两个一致标签不会重复计数；混合来源保留为组合来源，不伪装成human。

技术失败可保留原尝试后显式重试；完成（含abstain）不可覆盖或重试。改模型/规格应另建
固定预测计划，不混入同一结果包。raw_record保留补充原始记录，不被认证为真实提供商
签名回执；真实模型适配及安全持久化执行仍待后续包，不在本夹具中声称完成。

旧calibration-v1协议及所有旧工件原样保留，新路径纯读取/验证，不改数据库。
