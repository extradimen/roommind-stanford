# G5 首批真实开发预测完成

2026-09-12。用户明确授权将服务器既有Ollama凭据通过SSH送入Mac客户端内存，
仅用于本次开发校准；凭据未显示、未落盘、未进入命令参数或HTTP归档。
首次读取路径server/.env不存在，安全退出且未发模型请求；确认根目录.env后成功。
没有部署、推送、重启、修改服务器配置、邀请真人或启动旧/新资格批次。

## 结果与审计

- 固定输入48任务：完整旧G4.4的8对话×六维，4家族，非独立48样本。
- Ollama Cloud/gpt-oss:120b：51个HTTP请求，48完成，3技术失败后重试成功。
- 2次额外JSON字段不符合冻结协议；1次done_reason=length、eval_count=4096，按不完整响应处理。
- 不换模型、不提高上限、不覆盖完成结果；没有剩余失败或未决请求。
- 模型输出19 violation、29 clear、0 abstain；这不是19条已证实错误，更不是准确率。
- 全部请求、HTTP原始响应体、解析结果逐项绑定校验通过；HTTP头不归档。
- audit.json摘要：`d39c6d342f3a69e99d1cc049b6998ce1239410cd3e513b8edc1952cc14f98448`。
- 原194研究文件和核心源码摘要逐一未变。新辅助脚本不属于既有350核心测试回执。

attempt文件通过O_EXCL独立追加；同一案例已完成结果不再调用。合作文件锁防本机并发；
发现started而无HTTP记录须人工确认原进程退出及远端不确定性，不能自动重复推理。
这不是生产级分布式作业服务。真实执行进程已退出，无后台模型任务。
离线检查覆盖响应保存/已完成复用、认证及模型漂移停止、格式错误；无真实故障注入。

## 可以和不可以得出的结论

接入与原始证据链可以工作；冻结分类器仍会出现格式失败及可核实的语义理由错误。
定点审查发现它把quality_director的原话归给procurement_ally，据此作越权解释，见
`docs/G5_DEVELOPMENT_CALIBRATION_FIRST_REVIEW.md`。这里只证实该理由归属错误，
没有给整条对话造参考标签。缺少独立参考，不能计算误报、漏报或六维准确率。

这只是旧文本上的真实预测初查，不是G5四条件生成实验，不测G5认知/治理效果，
不能据此判断RoomMind胜过或不如Baseline。没有全量精读或真人校准。
下一本地工作是把证据message ID/字符引用/说话人绑定纳入独立新校准协议，保留本批
输入、原始响应和失败；新协议不得追溯替换这些结果。真实参考标注和确认研究仍未完成。
