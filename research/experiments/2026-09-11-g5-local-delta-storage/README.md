# G5 认知增量存储工程夹具（2026-09-11）

仅本地合成工程证据，非行为效应、真实模型校准或确认性研究。四组各40轮、12次可靠重开；
固定请求/回执预算100,000字节，共同观察窗口最近2项/2,048字符。未调用外部服务。
完整模型输出仍作为下一次认知状态和当前策略上下文；每轮生成后立即验证存储重建与实际
完整认知输出的 canonical JSON 精确相同。没有摘要替代原文，也没有删除原始模型回执。

新 manifest 显式声明 `g5-lossless-cognition-delta-v1`：每个角色首次及每16份状态做检查点，
其他状态选择较小的无损树/列表前缀差量或完整检查点。世界、角色、上一角色事件、前后状态
哈希及状态序号均绑定。旧协议和既有数据不迁移、不覆盖。

| 条件 | 事件数 | 实际认知编码字节合计 | 同一次运行完整认知状态字节合计 | 节省比例 | 完整源 JSON 字节 |
|---|---:|---:|---:|---:|---:|
| A | 40 | 0 | 0 | 不适用 | 357,646 |
| B | 40 | 769,431 | 2,859,445 | 73.09% | 1,348,303 |
| C | 40 | 0 | 0 | 不适用 | 437,322 |
| D | 40 | 769,431 | 2,859,445 | 73.09% | 1,427,979 |

节省比例仅比较**同一次运行**的实际认知字段编码与其精确解码后的逐轮完整状态总量；不把
新旧协议的哈希/事件ID当作相同，也不把该比例套用于整个数据库。源包还有事件、尝试、
重开回执等开销。检查点保留完整状态，因此不保证无限长程线性增长；当前读取仍验证历史，
未声明常数时间恢复或吞吐性能改善。A/C没有认知状态，不记作“节省100%”。

保留 `synthetic-world.sqlite`、四组 `*-source.json`、`*-profile.json`、`summary.json`。
Source 可用 `app.g5.artifacts.verify_source` 校验；`cognition_storage_profile.profile` 逐轮重建并
核对状态哈希，历史某轮的完整状态也可用只读 Snapshot 的角色观察重建。

summary 内容摘要：`28a2597a861cdd40e74edcced5b2023b324aaa70959a8f977d3d1a8e6c58c726`。

| 条件 | 完整源内容摘要 |
|---|---|
| A | c0c27ecf24dff3cb8615ebaa1671833262677d2b4f4c0da1528bb37eb4942b3f |
| B | 7fc622543899847324aa629a059040230c39323b3003dba7a3a3e7709d431b7d |
| C | aac907ef9dc0977e9d86cac7fd827a38af8143446e01e3ca8917a3f01c752a03 |
| D | e84c26b5b5b8a548f3df557f7d955194cfb51c6ac0b92b8e0eab96c7cdb56b8d |

以上为工件字段中对去除自身 `sha256` 的 canonical JSON 所作摘要，不是磁盘文件字节摘要。
以前的 `2026-09-11-g5-local-capacity` 及全部 G1–G4.16 工件原样保留。
